---
name: math-modeling
description: "Use this skill for ANY mathematical modeling competition task — CUMCM (国赛), MCM/ICM (美赛), GMCM (研赛/华为杯), Shenzhen Cup (深圳杯), MathorCup, APMCM, or any other mathematical modeling contest. Triggers include: mentions of '数学建模', 'mathematical modeling', '数模', '建模竞赛', '国赛', '美赛', 'MCM', 'ICM', 'CUMCM', competition problem statements, requests to solve modeling problems, '赛题', '建模', or any task involving building mathematical models for competition problems. Use this skill for the complete workflow from problem selection through final paper submission and defense preparation. This skill ensures AI autonomously executes the full competition workflow at special-prize quality level."
---

# 数学建模竞赛全流程自动化 Skill

> **核心定位**：本 Skill 是一个 AI 自主可执行的数学建模竞赛全流程约束系统。AI 严格按照本 Skill 的流程、检查点和验证协议执行，产出的论文在所有可客观度量的维度上达到国家级特等奖水平。
>
> **能力边界**：反直觉核心发现、数学美学 —— 这两项需要 AI 尽最大努力，通过系统性方法最大化产出概率。理论深度通过引用权威定理并做非平凡应用或推广来实现，不要求发明新定理。当深度不足时，提供一等奖保底路径。
>
> **完整参考**：`references/workspace.md`（约 383KB / ~15 万 tokens）包含全部阶段的详细操作指南、完整代码模板、质量评分表、竞赛适配器代码等。每个阶段的 ⛔强制前置步骤 指定了必须提前读取的具体章节。

---

## ⛔ 防跳步骤强制机制（16 道物理门禁，不可绕过）

> **核心原则**：AI 不能跳过、不能合并、不能浅层执行任何一个步骤。16 道锁从四个层级构成完整约束网——流程级锁（6道）确保阶段不跳过不合并 + 子步骤级锁（4道）确保每阶段内部深度执行 + 提交级锁（5道，含新增锁15/16）确保最终产出质量 + 篇幅级锁（1道，锁14）确保论文内容充实不浅薄。

**AI 必须严格遵守以下规则，违反任何一条则全流程无效：**

### 第一层：流程级锁（防跳过、防合并）  <!-- 锁编号说明：1-6(流程)→10-13(子步骤)→7-9(提交)→14(篇幅)，编号不连续是设计选择，非bug -->

**锁 1 — 文件门禁（内容深度版）**：每个阶段结束后，AI 必须用 Glob 验证产出文件已创建，**同时用 Grep 验证文件中包含所有必需的关键章节/内容**。仅文件存在但内容空洞 = 不通过。各阶段的内容验证关键词详见每个阶段的 🛑阶段门禁。

**锁 2 — 顺序锁（物理强制）**：阶段执行顺序严格为 0→1→2→3→4→5→6→6.5→7→8→9→10→11。前一阶段未通过全部检查点（文件门禁 + 内容深度门禁），后一阶段**绝对不可开始**。**物理强制机制**：进入阶段 N 之前，必须 `Grep 阶段进度追踪.md` 验证阶段 N-1 的标记为 `✅完成` + `Glob` 验证阶段 N-1 的产出文件全部存在。任何前置阶段标记为 `⏳` → 禁止进入。

**锁 3 — 阶段 5 逐问锁（物理强制）**：阶段 5 必须逐问求解。problem1.py 未通过检查点 → 禁止开始 problem2.py。禁止将多问合并为单一脚本。每问内部子步骤（5.0→5.0.5→5.1→5.2→5.3→5.4两轮→5.4.5→5.4.6→5.5→5.6→5.7→5.8）同样按序执行，不可跳跃。**物理强制机制**：进入 problemN.py（N≥2）之前，必须 `Glob` 验证 problem{N-1}.py 的产出图 ≥2 张 + CSV ≥1 个 + `Grep problem{N-1}.py` 中 ≥7/8 关键字存在。前置问题未达标 → 禁止创建下一个 problem 文件。

**锁 4 — 验证-论文锁（物理强制）**：阶段 6 和 6.5 的产出文件未全部创建 → 阶段 8（论文撰写）**绝对不可开始**。**物理强制机制**：进入阶段 8 之前，必须执行 `Glob` 验证以下 **7 个文件全部存在**：(1)`结果/figures/figS1_tornado.png` (2)`结果/figures/figS2_sobol.png` (3)`结果/figures/figS3_mc.png` (4)`结果/figures/figS4_hypothesis_relaxation.png` (5)`结果/figures/figS5_pareto.png` (6)统计检验报告 (7)`审稿意见与回复.md`。**任一缺失 → 绝对不可开始阶段 8**，必须回退阶段 6/6.5 补全。

**锁 5 — 上下文溢出主动断点（物理强制）**：当上下文不足以完成下一阶段时，必须主动生成 RESTORE.md 并停止。**物理强制机制**：(1)每个阶段开始时在 `阶段进度追踪.md` 追加 `⏱️ tokens预估:剩余[数字]K; 下阶段需[数字]K; 安全=[是/否]`。(2)任何一个 Write/PowerShell 输出 >5000字后，立即追加一行 `⏱️ tokens复查:剩余约[数字]K`。(3)阶段 N 门禁中 Grep 验证阶段 N 的 tokens 预估记录存在至少 1 行。欠缺 tokens 记录 = 门禁不通过。(4)余量=否 → 立即触发 RESTORE.md 断点。

**锁 6 — 下一阶段预读锁（物理强制）**：每个阶段门禁通过后、进入下一阶段之前，**必须先 Read SKILL.md 中下一阶段的全部内容**（含 ⛔强制前置步骤 + 🛠️工具调用表 + 执行任务 + 产出 + 🛑阶段门禁 + 检查点）。不预读 = 绝对不可开始下一阶段。**物理强制机制**：Read 完成后，必须在 `阶段进度追踪.md` 中追加行 `📖 已预读 SKILL.md L[起始行]-[结束行] 阶段N全部内容`（注明确切行号范围）。Grep 无此行号记录 → 视为未预读 → 禁止进入下一阶段。

### 第二层：子步骤级锁（防浅层执行、防偷懒）

**锁 10 — 阶段内容深度锁（含深度思考检查点）**：每个阶段的产出文件必须包含所有必需的内容章节（Grep 验证关键字）。**关键字存在 ≠ 内容有深度**——还需要通过 6 个深度思考检查点（CP1-CP6），强制 AI 在关键决策点外化推理过程。检查点回答写入 `阶段进度追踪.md`。**CP 反绕过验证标准**：每标记 ≥5 句，**每句 ≥10 个中文字符**，含 ≥1 处赛题原文引用（以中文引号 `""` 包裹），含 ≥1 个 ≥3 位有效数字的数值（`\d+\.\d{2,}`）。标记缺失或格式不达标 = 阶段门禁不通过。详见 `references/workspace.md` "深度思考检查点机制"。

Grep 关键字匹配要求：
- 阶段 0：`选题评估表` + `计算预判` + `歧义决策` + `附件文件清单` —— 4 项全匹配
- 阶段 1：`文献调研` + `权威定理匹配` + `三层追问` + `跨领域迁移` + `反套路设计` + `创新点产出` + `耦合分析` + `统一框架` + `陷阱识别` + `方法谱系图` —— 10 项中 ≥8 项匹配
- 阶段 2：`时间分配` + `里程碑` + `风险预案` —— 3 项全匹配
- 阶段 4：`假设链` + `反直觉假设` + `参数估计` + `95%` + `数据质量分级` —— 5 项全匹配
- 阶段 5（每问）：`模型建立` + `理论分析` + `模型求解` + `自批判` + `退化场景` + `公式合法性` + `消融实验` + `不确定性量化` —— 8 项中 ≥7 项匹配
- 阶段 8：`问题重述` + `模型假设` + `符号说明` + `模型建立与求解` + `验证与灵敏度` + `模型评价与推广` + `参考文献` —— 7 项中 ≥6 项匹配

**锁 11 — 阶段 5 子步骤顺序锁（物理强制——每子步骤有 Glob/Grep 可验证产物）**：阶段 5 内部每个子步骤必须有可验证的产出或记录，前一步未完成不得进入下一步：
- 5.0 决策树 → problemN.py docstring 中写明数学本质判断（Grep `"""` 块中 `数学本质` 关键字）
- 5.0.5 MVP baseline → CSV 中记录 baseline 指标（Glob `结果/*baseline*.csv` 存在 + Grep `baseline` 关键字）
- 5.4 两轮自批判 → 代码注释中记录批判和改进（Grep `# \[自批判.*第[12]轮\]` 模式至少 2 处）
- 5.4.5 退化场景 → 日志中记录退化测试结果（Glob `结果/log_extreme_test*.txt` 或 `结果/extreme_test_report.txt` 存在）
- 5.4.6 公式合法性 → 代码注释中记录 5 项检查（Grep `# \[公式合法性\]` 出现次数 ≥ 每问公式数 × 5）
- **物理强制**：阶段 5 门禁中逐项 Glob+Grep 以上产物，任一缺失 = 子步骤未执行 = 本阶段无效。

**锁 12 — 预读确认锁（物理强制）**：执行锁 6（预读）后，AI 必须在 `阶段进度追踪.md` 中追加一段"预读确认"——包含：(1)已读取的下一阶段名称 (2)识别的关键要求（≥3 条，每条 ≥15 字）(3)预估所需时间（分钟）。此确认是进入下一阶段的物理凭证，不存在 → 禁止进入下一阶段。**物理强制机制**：进入阶段 N 前，`Grep 阶段进度追踪.md` 验证存在 `预读确认.*阶段 N` 行 → 不存在 = 禁止开始。

**锁 13 — 阶段进度追踪锁（物理强制——防合并核心防线）**：AI 必须在工作目录维护 `阶段进度追踪.md` 文件，记录：当前阶段名称、开始时间、每个子步骤的完成状态（⏳/✅/⏭️）、门禁结果（Glob+Grep）、锁5 tokens 预估、锁12 预读确认。进入下一阶段时，**必须先将当前阶段标记为 `✅完成`**，**再**将下一阶段标记为 `🔜当前`。此文件是防合并的物理凭证：如果 AI 试图同时操作两个阶段（两个 `🔜当前` 同时存在），进度追踪文件暴露矛盾。**物理强制**：阶段 N 的 🛑门禁中增加 `Grep 阶段进度追踪.md` 验证 `🔜当前.*阶段 N` 唯一存在 + 所有前置阶段标记为 `✅完成`。

### 第三层：提交级锁（防最终产出缺陷）

**锁 7 — 结果溯源锁**：论文中每一个数值、每一张图表、每一个公式都必须可自动追溯到代码的具体输出文件。阶段 10 的 5 类一致性验证全部通过 → 才可提交。`consistency_report.txt` 的 Glob 确认 = 本锁的物理门禁。

**锁 8 — 赛题响应度锁**：提交前将赛题原文逐条拆分为所有显性+隐性要求，逐条标注论文中的回应位置（页码/行号/图表编号）。任何一条无回应 → **禁止提交**。`赛题响应度检查表.md` 的 Glob 确认 = 本锁的物理门禁。

**锁 9 — 图表-脚本绑定锁（含 3D 插图数量强制）**：论文中每一张图表必须有对应的、已成功运行的 Python 生成脚本。阶段 8 开始前，用 Glob 确认图表文件存在且 ≥5KB。缺失或空文件 → 禁止进入阶段 8。

**3D 插图数量强制子约束**（锁 9 子项，冲刺档）：特等奖论文必须包含 **≥12 张** 高质量的、与内容深度绑定的 3D 插图（非装饰性），按以下位置分配：
- 问题分析/数据概览：≥2 张（3D 数据地形图 + 3D 散点矩阵分布图——展示数据多维结构）
- 每问建模求解：≥2 张/问（3D 曲面图/优化景观 + 3D 轨迹/网络/流线图——将数学模型可视化）
- 验证与灵敏度：≥3 张（双参数 Sobol 3D 交互曲面 + MC 稳健性 3D 分布图 + Pareto 前沿 3D 曲面——验证深度三维化）
- 讨论/对比：≥2 张（3D 柱状图：多方法×多指标×多场景对比 + 文献定量对比 3D 柱状——多维视觉锚定）
- 结论与贡献：≥1 张（3D 瀑布图：创新点贡献度增量分解——收束全文的视觉高潮）

每张 3D 插图必须通过"信息增量验证"：关闭图注后仅看图 → 能否感知到数据/模型的关键结构特征？能 = 有效 3D，不能 = 纯装饰 → 不合格，重新设计。**信息增量客观化代理检查（防主观绕过）**：Grep 所有 3D 图生成代码中 `annotate\|text\|ax.text\|fig.add_annotation` 出现次数 ≥12 处（对应 ≥12 张 3D 图，每张至少标注 1 个关键数值）。不达标 → 3D 图未嵌入信息 → 不合格。**3D 图强制双视角**：每张 3D 图至少提供 45° 主视图 + 90° 俯视图，防止信息被视角遮挡。3D 文件命名必须含 `3d` 标识（如 `figN_3d_surface.png`），便于 Glob 统计。**3D 图总数 ＜ 12 张 → 阶段 5 门禁不通过 → 禁止进入阶段 6**。**单题（N=1问）特殊约束：3D 最低要求 max(12, 8+2N) = 12 张（问题分析可增至≥3张、验证增至≥4张、讨论增至≥3张以补足差额）**。

**锁 14 — 论文篇幅锁（Label 精确定位 + 按章诊断回退）**：

**页数精确定位方法**（不再用 PyPDF2 猜附录位置）：
1. 在 `main.tex` 中 `\section{结论}` 末尾插入 `\label{lastmain}`，紧接着是参考文献节（无分页符）。参考文献结束后 `\newpage` 分页，然后 `\appendix`
2. 页面结构：摘要(第1页) → 正文(问题重述→结论, 第2页至第P页) → 参考文献(第P+1页起, 紧接结论) → `\newpage` → 附录(无限页)
3. xelatex 编译后，Grep `.aux` 中 `\newlabel{lastmain}` → 提取页码 P → **正文页数 N = P − 1**（P 含摘要第1页）
4. 参考文献从第 P+1 页开始，页数不限制（但属于正文后的独立部分）。附录从参考文献后的 `\newpage` 开始，页数不限制（必须完整含全部代码+README+requirements.txt，文件总大小 ≤20MB）

**篇幅判定与回退机制**：

| 正文页数 N = P−1 | 判定 | 操作 |
|:--:|------|------|
| N < 25 | 🔴 严重不足 | 问题可能在建模深度或赛题理解 → 回退阶段 5/1 重新审视 |
| 25 ≤ N < 29 | 🟡 深度不足 | 回退阶段 8，用"篇幅不足诊断表"逐章定位薄弱章节并扩充（见下方）|
| N = 29-30 | 🟢 达标（需防撑页数） | 执行防撑页数检查（Grep 全宽图比例 ≥30% → 缩小重编译 → 仍 ≥29 则通过）|
| N > 30 | 🟠 超标 | 精简正文或移至附录（禁止直接删内容——优先压缩图表至 ≤3/4 页或移至附录）|

**篇幅不足诊断表**（正文 < 30 页时，回退阶段 8 前先逐章诊断）：

| 章节 | 目标页数 | Grep 诊断当前状态 | 不足时的扩充操作 |
|------|:--:|------|------|
| §1 问题重述 | 2-3 页 | 段落数 < 5 → 不足 | 增加数据概览图 + 每问的数学化重述 + 已有方法局限引用 |
| §2 问题分析 | 2-3 页 | 段落数 < 5 → 不足 | 补充技术路线图说明 + 方法谱系图分析 + 耦合关系图解读 |
| §3 模型假设 | 1.5-2 页 | 假设条数 < 5 → 不足 | 每条增加必要性+合理性+违反后果三要素论证 |
| §4 符号说明 | 0.5-1 页 | 仅表格无说明 → 不足 | 增加分类说明段 + 命名规则解释 |
| §5 建模求解（每问） | 4-6 页/问 | 公式数 < 5/问 → 不足 | 补充推导中间步骤 + 算法伪代码分析 + 更多机理解释 |
| §6 验证分析 | 3-5 页 | 图表 < 5 → 不足 | 增加假设松弛曲线 + Pareto前沿 + 对比总表分析 |
| §7 讨论 | 2-3 页 | 段落数 < 6 → 不足 | 扩充文献对比（≥3篇定量）+ What-if 实验分析 + 局限诚实报告 |
| §8 模型评价 | 1-2 页 | 段落数 < 4 → 不足 | 增加推广条件量化 + 适用边界精确描述 |
| §9 结论 | 1-1.5 页 | 段落数 < 3 → 不足 | 增加贡献编号列表 + 核心数值汇总表 + 未来方向具体化 |

**回退循环控制**：
- 每次回退阶段 8 → 用诊断表定位不足章节 → 扩充 → 重新编译 → 重新验证
- 最多 3 次循环。第 3 次后仍 < 30 页 → 触发系统性问题诊断：
  - 检查是否赛题理解偏差（回退阶段 1）
  - 检查是否模型过于简化（回退阶段 5）
  - 若确认无误但确实无法达到 30 页（如赛题仅 1 问）→ 在论文 §7 中诚实说明"本题的数据/问题规模限制了分析展开的深度"

**附录规则**：附录页码无上限，但必须完整（≥8 个 .py 文件代码 + README + requirements），附录总页数 ÷ 正文页数 ≤ 60%（防止附录过长喧宾夺主），文件总大小 ≤20MB。

**锁 15 — 提交材料完整性锁（防最后一公里翻车）**：提交前必须逐项核对赛事官方提交规范，产出 `提交包完整性检查表.md`，包含：(1)论文 PDF 文件命名是否正确（国赛：`选题编号+队号.pdf`；美赛：`队号.pdf`）；(2)支撑材料命名与格式（国赛：RAR/ZIP，含全部代码+数据+图表，≤20MB）；(3)论文中无参赛者姓名/学校/赛区信息（Grep 验证中文字段）；(4)MD5 校验值已记录（防文件损坏）。**物理强制**：`Glob 提交包完整性检查表.md` 存在 + Grep 4/4 项标记 `✅` → 才可提交。任一 `❌` → 禁止提交。

**锁 16 — 附录代码可运行性独立验证锁（防"论文数值与代码不一致"）**：阶段 10 最终重跑验证后新增独立步骤——(1)从 `main.tex` 附录中提取全部 `.py` 代码 → 写入独立文件 `代码/附录提取/`；(2)清空 `结果/` 缓存 → 从头运行附录提取代码；(3)对比运行输出与论文中的全部数值（≥4 位有效数字逐对核对）；(4)任一不一致 → 修正论文或代码 → 重新提取 → 重新运行 → 重新核对。**物理强制**：`Glob 附录代码可运行性验证报告.md` 存在 + Grep `全部一致` → 才可进入阶段 10 门禁。不一致 → 禁止提交。

---

## 🚀 启动指令

当用户提出数学建模任务时，按以下流程启动：

```text
1. 读取赛题原文与数据
2. 执行「AI 能力自评」
3. 执行「竞赛类型自动识别」（Read references/workspace.md 赛事差异化指南）
4. 按阶段 0 → 1 → ... → 11 严格顺序执行
5. 每阶段结束后：用 Glob 工具验证所有产出文件已创建
6. 文件验证通过后：执行检查点自检，全部通过后才可进入下一阶段
7. 文件验证或检查点未通过：回退到对应阶段重新执行，不得跳过
```

**用户输入模板**（用户将以下信息提供给 AI）：

```text
赛题：[粘贴赛题原文]
数据路径：[数据文件路径]
赛事类型：[国赛/美赛/华为杯/深圳杯/其他]
题目类型：[A/B/C/D/E/F]
```

---

## 🔧 AI 能力自评（阶段 0 之前强制执行）

AI 在开始执行前，必须逐项评估自身能力（✅ 可用 / ⚠️ 受限 / ❌ 不可用）：

| 能力项 | 评估方式 | 影响 |
|--------|---------|------|
| **代码执行** | 能否直接运行 Python 并获取输出？ | 决定模型求解和验证的自动化程度 |
| **LaTeX 编译** | 能否执行 xelatex/pdflatex？ | 决定论文编译是否自动化 |
| **文件读写** | 能否读取 CSV/XLSX/PDF 数据文件？ | 决定数据处理方式 |
| **文献检索** | 能否访问 WebSearch/WebFetch？ | 决定能否扩充参考文献来源 |
| **上下文窗口** | 估算可用 tokens 数量 | 决定是否需要分段执行 |

**执行模式**：AI 必须全自主完成所有阶段。如果某项能力不可用（如无法执行代码），AI 应尽最大努力生成完整可运行代码，并通过其他可用工具（如文件写入、命令行调用）完成任务。不依赖人工干预。

---

## 📐 上下文管理策略

本工作区设计为在有限上下文窗口内完成全流程。

**Tier 1（始终在上下文，约 20K tokens）**：
- 9 大核心原则（见下方）
- 阶段速查表（阶段-任务-产出-检查点）
- 当前进度卡 + 关键决策快照（最近 5 个）
- 赛事适配参数摘要

**Tier 2（按需加载，从 `references/workspace.md` 读取）**：
- 当前阶段的详细操作手册
- 相关代码模板（`templates/` 目录）
- 详细质量标准和评分细则

**上下文溢出预测阈值**：
- 剩余 > 40%：正常执行
- 剩余 20-40%：精简冗余输出
- 剩余 10-20%：主动生成 `RESTORE.md` 断点
- 剩余 < 10%：立即保存状态，停止执行

**RESTORE.md 断点格式**（7 段强制结构）：
1. 当前位置（阶段N/名称 + 卡点）
2. 不可丢失的决策链（[D{n}] 压缩格式，每条 ≤200 tokens）
3. 数据快照（路径 + 关键统计量 + 已完成预处理）
4. 模型快照（类型 + 参数 + 已验证性能）
5. 已完成的产出（阶段-文件-状态表）
6. 下一步精确操作（含文件路径和函数名）
7. 恢复指令（一句话粘贴到新对话即可）

---

## 🧠 九大核心原则（顺序 = 优先级）

### 原则 1：深度优先（Depth First）
对关键问题深挖到底。问"为什么"至少三层。真正的一等奖论文在于对关键点的深刻洞见，而非面面俱到。

### 原则 2：证据驱动（Evidence Driven）
每个判断必须有依据。"我认为..." → 禁止；"因为题目条件 X，所以..." → 可以；"数据中 Y 呈现 Z 分布，这意味着..." → 最优。

### 原则 3：矛盾敏感（Contradiction Sensitive）
主动寻找矛盾和异常——它们是创新的源泉。数据与预期不符 → 深挖原因；两个结论矛盾 → 分析来源；结果"太好" → 检查 bug/过拟合。

### 原则 4：审稿人视角（Reviewer Perspective）
每完成一部分，切换为审稿人视角："如果我是评委，我会问/反驳/质疑/怎么验证？"提前回答这些问题，比事后补救强 10 倍。

### 原则 5：跨领域联想（Cross-Domain Association）
每遇困境，问："其他领域有没有类似问题？怎么解决的？数学结构同构于什么著名问题？"这是创新的最可靠来源。

### 原则 6：自我否定（Self-Negation）
对自己的第一个方案保持怀疑。主动找最大弱点。无法修补 → 果断放弃，启用备选。不要因为做了很多工作就舍不得放弃（沉没成本谬误）。

### 原则 7：可证伪性（Falsifiability）
每个假设必须可被数据反驳。"如果 H 成立→应观察到 P；如果观察到非 P→H 被反驳。"无法证伪的假设 = 无信息量的假设 = 应删除。

### 原则 8：反偷懒深度强制（Anti-Shortcut Enforcement）
- **三级证据链**：每个建模决策必须附带 (1)问题结构论证 (2)数据验证 (3)对比实验
- **MVP 先行**：复杂模型前先用最简方法跑 baseline
- **"为什么没人做"答辩**：每个创新点必须回答"在文献中是否已被使用？""有什么隐藏代价？""在什么条件下失效？"
- **禁止填空式产出**：模板是骨架，必须用赛题实质内容填充

### 原则 9：幻觉自检与零容忍（Zero Hallucination）
四类零容忍：(1)数值结果幻觉——必须可溯源到代码输出；(2)文献幻觉——每篇标注置信度和验证方法；(3)代码行为幻觉——禁止声称运行但未实际执行；(4)公式幻觉——每个公式必须有推导或来源。

---

## 📋 全流程阶段执行手册

> **使用方式**：每个阶段开始时，AI 必须先执行 ⛔强制前置步骤（用 Read 工具读取 workspace.md 指定章节），再执行本表中的"执行指令"。检查点通过后才能进入下一阶段，未通过则回退修正。

---

### 阶段 0：选题决策与计算可行性预判

**执行指令**：
0. **📎 附件文件清单确认（首位执行）**：比赛题目常附带以下附件，必须逐一确认是否已获取并读取：
   - 赛题正文（PDF/Word）
   - 数据文件（CSV/XLSX/TXT）
   - 补充说明 PDF（含数据字典、建模约束、评分细则等）
   - 如有任何附件尚未读取 → 要求用户提供路径 → Glob 确认文件存在 → Read 读取全部附件内容
   - 产出"附件文件清单"（文件名+类型+大小+关键内容摘要），确保无遗漏
1. 如为多选题赛事（国赛 A/B/C、美赛 6 题），执行五维选题评估；单题赛事跳过
2. 估算数据规模 + 模型复杂度 + 蒙特卡洛/Sobol → 预估总计算时间
3. 识别题目中的歧义表述（"综合考虑""合理假设"等开放性措辞）
4. 产出 `选题决策记录.md`（含选题评估表 + 计算预判 + 歧义决策 + 附件文件清单）

**检查点**：预估计算时间 < 剩余时间的 50%

**⛔ 强制前置步骤（不执行则本阶段无效）**：先用 Read 工具读取 `references/workspace.md` 的对应章节（搜索"阶段 0：选题决策"和"竞赛通用自动适配器"）后，才能开始本阶段。

**🛑 阶段门禁（文件+内容验证）**：完成后必须执行双重验证——① Glob 验证 `选题决策记录.md` 已创建；② Grep 验证文件中包含全部 4 个关键字：`选题评估表`、`计算预判`、`歧义决策`、`附件文件清单`。任一缺失=本阶段无效，禁止进入阶段1。

> ⛔ **下一阶段预读锁（防忘机制）**：本阶段门禁通过后 → **必须立即 Read SKILL.md 中 阶段 1 的全部内容**（含 ⛔强制前置 + 🛠️工具调用 + 执行任务 + 产出 + 🛑门禁 + 检查点）。不预读 = 禁止开始 阶段 1。

---

### 阶段 1：赛题深度分析

**执行指令**：
0. **🔍 文献调研（建模前必须完成）**：在深度分析题目之前，先系统梳理已有方法的类型、核心思想和适用条件。产出"已有方法分类矩阵"（方法类型→核心思想→适用条件→在本题条件下的局限性）。至少找到 **1 个创新空白**（已有方法无法覆盖的方向）。此步骤决定后续模型选择的方向，不等同于阶段 8.6 的论文文献综述写作。
0.5. **📐 权威定理匹配（特等奖关键——理论深度的源头）**：识别每问数学结构后，从以下定理库中系统匹配可应用的权威定理，产出"定理候选清单"。每个候选附：(1)定理标准陈述 (2)标准适用条件 (3)本题条件与标准条件的差异 (4)非平凡应用方式（推广定理条件/精细化工上界/构造性应用/多定理联合）。目标：为每问找到 ≥1 个可非平凡应用的定理。

**权威定理库（按数学结构分类）**：
| 数学结构 | 候选定理 |
|---------|---------|
| 优化问题 | Kuhn-Tucker 条件、Lagrange 对偶定理、Slater 条件、Karush-Kuhn-Tucker 定理、Wolfe 对偶 |
| 不等式/估计 | Jensen 不等式、Cauchy-Schwarz、Hölder 不等式、Kantorovich 不等式、Cramér-Rao 下界 |
| 不动点/存在性 | Banach 不动点定理、Brouwer 不动点定理、Schauder 不动点、Kakutani 不动点 |
| 稳定性/收敛 | Lyapunov 稳定性定理、LaSalle 不变性原理、Perron-Frobenius 定理、Gershgorin 圆盘定理 |
| 概率/统计 | Hoeffding 不等式、Bernstein 不等式、Dvoretzky-Kiefer-Wolfowitz 定理、Rao-Blackwell 定理 |
| 微分方程 | Picard-Lindelöf 存在唯一性、比较原理、极大值原理、Fredholm 择一性 |
| 图论/组合 | Hall 婚姻定理、Tutte 定理、Menger 定理、Euler 公式、Ramsey 定理 |
| 信息论 | Shannon 熵最大化、数据处理不等式、Fano 不等式、率失真定理 |
| 博弈论 | Nash 均衡存在性、von Neumann 极小极大定理、Zermelo 定理 |

**非平凡性判定标准**（满足任一即"非平凡"）：
- 定理的标准条件不完全满足 → 需要推广或验证条件可放松
- 定理给出的界/估计过松 → 需要精细化工或紧化
- 多定理联合使用得出单一定理无法得出的结论 → 构造性组合
- 定理应用于非标准领域（定理原是 X 领域的，迁移到 Y 领域使用）

1. **三层追问**：表面理解（每问做什么？输入/输出/评价标准？）→ 数学本质（每问的数学结构类型？目标函数/决策变量？）→ 深层洞察（隐含矛盾？反直觉之处？去掉约束后的问题？）
2. **跨领域迁移**：对 8 个领域（物理/生态/经济/流行病/运筹/控制/信息/博弈）逐一排查可迁移方法。至少找到 **3 个可行迁移方向**
3. **标准方法批判**：对每问的教科书解法，回答"核心假设→是否成立→偏差→修正方案"
4. **🔄 反套路设计（特等奖关键）**：对步骤 3 中每种标准解法执行"6 维变换"——确定性→随机鲁棒、静态→动态演化、单目标→多目标 Pareto、参数预设→数据驱动自适应、黑箱求解→问题结构定制算法、独立求解→耦合联动。每个问题设计 ≥2 个反套路方案。详见 `references/workspace.md` 反套路设计指南（5 步生成器 + 赛题类型速查）。
5. **创新点产出**：≥3 个创新点，每个含"来源→核心思想→理论依据→实现路径→预期效果→验证方法→失败预案"
6. **耦合分析**：画问题耦合关系有向图（数据耦合/模型耦合/结果耦合/理论耦合）
7. **统一框架构建**（P2 特等奖）：尝试构建统一数学框架；如各问数学结构不可调和，使用分层递进框架替代
8. **赛题陷阱识别**：检查 5 类常见陷阱（假复杂真解析/隐约束/数据冗余/公式有误/评价歧义）
9. 画出**完整技术路线图**（含每个决策点的备选方案）
10. 画出**方法谱系图**（Method Genealogy）——展示"本文方法在文献中的分类定位"的树形/时间线/二维散点分类图。至少 2 个分类维度，每个节点标注代表性文献，本文方法用红色★突出标注。详见 `references/workspace.md` "方法谱系图"。

**产出**：`赛题分析.md`（含以上全部内容 + 技术路线图 + 方法谱系图）

**⛔ 强制前置步骤（不执行则本阶段无效）**：先用 Read 工具读取 `references/workspace.md` 的对应章节（搜索"阶段 1"全部子节和"跨领域方法迁移引擎"和"反套路设计指南"和"论文标题设计指南"）后，才能开始本阶段。

**🛑 阶段门禁（文件+内容+深度验证）**：完成后必须执行三重验证——① Glob `赛题分析.md`；② Grep ≥8/10 关键字（文献调研/权威定理匹配/三层追问/跨领域迁移/反套路设计/创新点产出/耦合分析/统一框架/陷阱识别/方法谱系图）；③ Grep `阶段进度追踪.md` 中 `[CP1]` 和 `[CP2]` 标记存在且每标记 ≥5 句，每句 ≥10 个中文字符，含 ≥1 处赛题原文引用（以中文引号 `""` 包裹），含 ≥1 个 ≥3 位有效数字的数值（`\d+\.\d{2,}`）。缺失或格式不达标=深度思考检查点未执行=本阶段无效，禁止进入阶段2。

> ⛔ **下一阶段预读锁（防忘机制）**：本阶段门禁通过后 → **必须立即 Read SKILL.md 中 阶段 2 的全部内容**（含 ⛔强制前置 + 🛠️工具调用 + 执行任务 + 产出 + 🛑门禁 + 检查点）。不预读 = 禁止开始 阶段 2。

---

### 阶段 2：项目规划

**执行指令**：
1. 按时间分配模板（国赛 72h / 美赛 96h）制定计划，预留 20% 缓冲 + 1.5h 最终缓冲
2. 标注 5 个关键里程碑（M1 选题完成 → M2 建模完成 → M3 验证完成 → M4 初稿完成 → M5 终稿检查）
3. 识别 3 个主要风险点并准备预案

**产出**：`建模计划.md`

**⛔ 强制前置步骤（不执行则本阶段无效）**：先用 Read 工具读取 `references/workspace.md` 的对应章节（搜索"阶段 2"和"团队协作指南"）后，才能开始本阶段。

**🛑 阶段门禁（文件+内容验证）**：完成后必须执行双重验证——① Glob 验证 `建模计划.md` 已创建；② Grep 验证文件中包含全部 3 个关键字：`时间分配`、`里程碑`、`风险预案`。任一缺失=内容不完整=本阶段无效，禁止进入阶段3。

> ⛔ **下一阶段预读锁（防忘机制）**：本阶段门禁通过后 → **必须立即 Read SKILL.md 中 阶段 3 的全部内容**（含 ⛔强制前置 + 🛠️工具调用 + 执行任务 + 产出 + 🛑门禁 + 检查点）。不预读 = 禁止开始 阶段 3。

---

### 阶段 3：环境准备

**执行指令**：
```bash
pip install -r requirements.txt  # 使用 templates/requirements.txt
```
同时创建标准目录结构（题目/代码/结果/论文/答辩）。

**产出**：依赖就绪 + 目录结构

**⛔ 强制前置步骤（不执行则本阶段无效）**：先用 Read 工具读取 `references/workspace.md` 的对应章节（搜索"阶段 3"和"代码架构规范"和"版本控制要求"）后，才能开始本阶段。

**🛑 阶段门禁（文件验证）**：完成后必须用 Glob 验证 `requirements.txt` 和 `代码/` 目录已创建。缺失=本阶段无效，禁止进入阶段4。

> ⛔ **下一阶段预读锁（防忘机制）**：本阶段门禁通过后 → **必须立即 Read SKILL.md 中 阶段 4 的全部内容**（含 ⛔强制前置 + 🛠️工具调用 + 执行任务 + 产出 + 🛑门禁 + 检查点）。不预读 = 禁止开始 阶段 4。

---

### 阶段 4：数据驱动洞察

**执行指令**：
1. **4.0 预处理管线**（强制执行）：
   - 编码检测 → 多编码尝试加载 → 列名规范化 → 类型检查 → 缺失值诊断
   - 缺失值处理决策树（按缺失率分 4 档：<5%/5-20%/20-50%/>50%）
   - 异常值检测（IQR + Z-score + 孤立森林三方法交叉确认）
   - 产出 `data_manifest.json` + `preprocessing_log.txt`
2. **4.1 质量报告**：行列数/类型/缺失/异常值汇总
3. **4.2 深度 EDA 四层**：单变量分布拟合（≥3 种分布 + AIC/BIC）→ 双变量关系（3 种相关系数不一致=非线性信号）→ 多变量结构（PCA/t-SNE）→ 网络特性（如适用）
4. **4.3 洞察→假设转化**：每条发现的规律 → 生成机制假设 → 可检验推论 → 模型设计启示。至少 **3 条完整假设链**
5. **4.4 参数估计**：每个关键参数给出数据驱动估计值 + 95% CI
6. **4.5 反直觉假设生成器（特等奖关键）**：基于 EDA 结果，强制列出 **5 个反直觉假设**——"如果只看数据表面，我会认为 X，但实际上数据暗示了相反的趋势 Y"。即使其中大部分最终被数据证伪，这个步骤强制 AI 跳出惯性思维，最大化反直觉发现的概率。
7. **4.6 数据质量分级与降级策略**：对数据质量进行 A/B/C/D 四级判定（A=良好→标准流程，B=可处理→强化预处理，C=困难→改用鲁棒方法+先验约束，D=极差→从预测转为趋势判断/定性分析）。D 级数据需在论文 §3 或 §7 中附"极差数据声明模板"。详见 `references/workspace.md` "极差数据场景的系统性建模策略"。

**产出**：`数据洞察报告.md` + fig1~N

**⛔ 强制前置步骤（不执行则本阶段无效）**：先用 Read 工具读取 `references/workspace.md` 的对应章节（搜索"阶段 4"和"4.0 数据预处理标准管线"）后，才能开始本阶段。

**🛑 阶段门禁（文件+执行日志+深度验证）**：完成后必须执行三重验证——① Glob 6 个文件全部存在 + Glob `结果/log_data_preprocessing.txt` + `结果/log_stage4_eda.txt` 全部存在且含正常结束标记；② Grep `阶段进度追踪.md` 中 `[CP3]` 标记存在且 ≥5 句，每句 ≥10 个中文字符，含 ≥1 处赛题原文引用，含 ≥2 个 ≥3 位有效数字的具体数值（如 Spearman ρ=0.73）；③ Grep 本阶段 `⏱️` tokens 预估行 ≥1 处。全通过 → 进入阶段5。

> ⛔ **下一阶段预读锁（防忘机制）**：本阶段门禁通过后 → **必须立即 Read SKILL.md 中 阶段 5 的全部内容**（含 ⛔强制前置 + 🛠️工具调用 + 执行任务 + 产出 + 🛑门禁 + 检查点）。不预读 = 禁止开始 阶段 5。

---

### 阶段 5：建模求解（逐问执行）

**执行指令**（复制 `templates/problem_template.py` 并按以下子阶段逐步填充）：

1. **5.0 决策树（增强：含多模型系统选择框架）**：先判断每问的数学本质，按决策树选择模型类型。**当多种候选方法结果不同时，使用定量决策矩阵**（6 维度加权评分：精度25%+可解释性20%+效率15%+鲁棒性15%+扩展性10%+匹配度15%）选出最优方法。总分最高者胜出。总分差距≤0.5→两种都实现并对比。产出 `stage5_model_selection.md` 记录决策矩阵。详见 `references/workspace.md` "多模型系统选择框架"。
2. **5.0.1 专项指南**：如为 A 题（微分方程），参照 ODE/PDE 建模标准流程 + 网格独立性检验
3. **5.0.2 计算量预估**：先估算单次模型调用时间，按 6 档动态调整 Sobol 和 MC 次数
4. **5.0.5 MVP 先行**：用最简方法（线性回归/LP/贪心）跑出 baseline，记录核心指标
5. **5.1 建模**：完整公式推导 (假设→公式) + 假设必要性论证 + 适用条件
6. **5.2 理论分析**：选 ≥2 项（收敛性/复杂度/误差界/稳定性），有推导过程不堆砌术语
7. **5.3 算法**：≥2 种不同原理方法对比 + 参数设置依据 + 收敛诊断。**每问 ≥1 个 `algorithm2e` 格式伪代码**（含 Input/Output 声明 + 行号 + 复杂度标注 + `\label{alg:xxx}` 引用标签）。详见 `references/workspace.md` "算法伪代码 LaTeX 排版规范"。
8. **5.3.5 模型失败恢复协议（新增强制）**：建模过程中遇到任何失败（代码报错/不收敛/结果差/矛盾/分歧），必须按 5 级诊断树（L0-L4）系统排查 + 记录到"模型失败恢复日志"。严禁掩盖失败、假装顺利。详见 `references/workspace.md` "模型失败诊断与恢复协议"。
9. **5.4 自批判（两轮）**：审稿人视角批判假设/稳健性/结果/局限性 → 改进 → 再批判
10. **5.4.5 退化场景验证（防"能跑但错"）**：每问求解后强制执行——构造至少 1 个"已知正确答案"的退化/简化场景（如参数取极值/数据退化到特例/假设简化到教科书情形），验证模型输出与预期一致。报告：(1)简化场景描述 (2)预期结果 (3)实际输出 (4)一致性判断。此步骤防止代码运行无报错但逻辑错误的隐蔽缺陷。

11. **5.4.6 公式数学合法性验证（防"形式上对但实质上错"）**：每个编号公式必须通过以下 5 项检查：
1. **量纲一致性**：等式两边量纲是否一致？（对物理/工程类问题强制执行）
2. **边界行为合理性**：参数取边界值(0/∞/负数/极大值)时公式行为是否符合物理/数学直觉？
3. **特例退化检验**：公式退化为已知特例（如均匀分布、对称情况、线性近似）时是否与已知结果一致？
4. **奇点检查**：公式在定义域内是否存在未声明的奇点/不连续点？
5. **数值插值验证**：随机取 5 组参数，用公式计算 vs 用独立方法（如蒙特卡洛、有限差分）验证，相对误差 <1%

报告格式：公式编号 | 5项检查结果 | 不通过则修正后重检
12. **5.5 创新实现**：每个创新点有消融实验 + 量化贡献 + 统计显著性
13. **5.6 结果**：数值 ≥5 位有效数字 + CI + 多维度解读
14. **5.7 不确定性量化**：参数(±20%) + 模型(≥2 种设定) + Bootstrap(≥1000 次) 三维度
15. **5.8 机理解释三层**：数学层(公式结构) → 数据层(分布特征) → 领域层(实际意义)

**产出**：problemN.py（含代码注释溯源标签——每个 ≥2 位数字的数值必须有 `# [数据驱动]`/`[理论推导]`/`[文献参考]`/`[退化校验]`/`[物理约束]`/`[竞赛规则]`/`[保守选择]` 7 种溯源标签之一）+ 每问 ≥6 张高质量图 + 1-3 个结果 CSV。详见 `references/workspace.md` "代码注释溯源强制规范"。

**代码模板**：`templates/problem_template.py`（含全部 7 个标准函数签名 + docstring）

**⛔ 强制前置步骤（不执行则本阶段无效）**：先用 Read 工具读取 `references/workspace.md` 的对应章节（搜索"阶段 5"全部子节和"5.0.1"和"5.0.2"和"模型失败诊断与恢复协议"和"算法伪代码 LaTeX 排版规范"和"多模型系统选择框架"和"ODE/PDE求解与代码实现"和"5.4.5 退化场景验证"和"5.4.6 公式数学合法性验证"和"每个问题脚本必须包含"）后，才能开始本阶段。

**🛑 阶段门禁（文件+内容+子步骤验证）**：完成后必须执行三重验证——

① **Glob 文件验证**：`problem1.py`（如有多问则还要 `problem2.py`/`problem3.py`/`problem4.py`）全部存在 + 每问 ≥6 张高质量图（含原理图+消融对比图+结果图+不确定性图+收敛曲线图+额外高级图各≥1张）+ 每问 ≥1 个结果 CSV。**图表总量 ≥22 张**（不含附录）。**3D 插图总量 ≥12 张**（问题分析≥2 + 每问≥2×N + 验证≥3 + 对比≥2 + 结论≥1；每张 ≥5KB 且命名含 `3d` 标识，如 `figN_3d_surface.png`）。任一缺失=本阶段无效。

② **Grep 内容深度验证**：对每问的 problemN.py 执行 Grep 验证，≥7/8 个关键字必须存在：`model_building`（模型建立）、`theoretical_analysis`（理论分析）、`model_solving`（模型求解）、`自批判`或`self_critique`、`退化场景`或`degenerate`、`公式合法性`或`formula_check`、`ablation_study`（消融实验）、`uncertainty_quantification`（不确定性量化）。不足 7 个=子步骤缺失=本阶段无效。

③ **锁 11 子步骤验证**：problemN.py 的 docstring 中必须写明数学本质判断（5.0）+ MVP baseline 指标出现在结果 CSV 中（5.0.5）+ 代码注释中存在两轮自批判记录（5.4）+ 退化场景测试日志存在（5.4.5）+ 公式 5 项检查注释存在（5.4.6）。

④ **Glob 执行日志验证**：`Glob "结果/log_problem*.txt"` 全部存在 + 每文件 Grep `✅\|完成\|SUCCESS` 存在正常结束标记。N 问 → N 个日志文件。缺失或无结束标记 = 脚本未实际运行 = 本阶段无效。
⑤ **Grep 深度检查点**：`阶段进度追踪.md` 中 `[CP4]` 和 `[CP5]` 标记存在且每标记 ≥5 句，每句 ≥10 个中文字符。CP4 含 ≥2 个 ≥3 位有效数字的数值（`\d+\.\d{2,}`）并解释放弃方法的具体定量理由。CP5 含修改前后的量化对比且差异 ≥5%。缺失或格式不达标=本阶段无效。
⑥ **锁5 tokens复查**：Grep 阶段进度追踪.md 中存在本阶段的 `⏱️` 行 ≥1 处。无 tokens 记录 = 锁5未执行 = 本阶段无效。

全部验证通过 → 阶段有效 → 进入阶段6。

> ⛔ **下一阶段预读锁（防忘机制）**：本阶段门禁通过后 → **必须立即 Read SKILL.md 中 阶段 6 的全部内容**（含 ⛔强制前置 + 🛠️工具调用 + 执行任务 + 产出 + 🛑门禁 + 检查点）。不预读 = 禁止开始 阶段 6。

---

### 阶段 6：验证与灵敏度分析

**执行指令**（使用 `templates/sensitivity_analysis.py`）：

1. **多方法交叉验证**：
   - 残差分析 (Shapiro-Wilk + Durbin-Watson)
   - 分层 K 折交叉验证 (K=10, 重复 5 次)
   - ≥3 种不同原理方法对比 + 统计显著性检验
2. **统计检验**（使用 `templates/statistical_tests.py`）：t/Mann-Whitney + Friedman + Bonferroni 校正 + 检验力分析
3. **灵敏度分析**：
   - 单因素 ±10%/±20%/±50% → 归一化灵敏度系数 → Tornado 图
   - Sobol 全局灵敏度：一阶 + 总阶指数
   - 双因素交互等高线图
4. **蒙特卡洛稳健性**：次数按 5.0.2 动态调整 + 分布图 + 最坏情况 + 95%/99% CI
5. **假设灵敏度分析（特等奖关键）**：对 2-3 条最可能被违反的核心假设进行灵敏度测试——构造假设被违反的替代场景，对比原始假设 vs 替代假设下的结果差异，量化结论的稳定性。详见 `references/workspace.md` "假设灵敏度分析"章节。
6. **假设松弛分析（特等奖关键）**：逐条放松假设（0%完全满足→100%完全违反），绘制"性能退化曲线"——展示模型如何优雅退化而非突然崩溃。至少 2 条假设的退化曲线 + "可接受退化线""不可接受退化线"标注。详见 `references/workspace.md` "假设松弛分析"。
7. **复杂度-性能 Pareto 前沿分析**：至少 3 个不同复杂度版本（简单→中等→复杂→本文），绘制 Pareto 前沿图（X=复杂度，Y=性能），标注边际收益递减点，论文中回答"增加的复杂度是否值得"。详见 `references/workspace.md` "模型复杂度-性能权衡分析"。

**产出**：灵敏度图（含 Tornado+Sobol+MC+假设松弛退化曲线+Pareto前沿）+ 统计检验报告 + 结果 CSV

**⛔ 强制前置步骤（不执行则本阶段无效）**：先用 Read 工具读取 `references/workspace.md` 的对应章节（搜索"阶段 6"和"5.0.2 计算量动态调整"和"统计显著性标注代码片段"和"统计检验代码模板"和"假设灵敏度分析"）后，才能开始本阶段。

**🛑 阶段门禁（文件+执行日志验证）**：完成后必须用 Glob 验证：(1)≥5 张灵敏度图（Tornado + Sobol一阶/总阶 + MC分布 + 假设松弛退化曲线 + Pareto前沿）；(2)统计检验报告；(3)`结果/log_sensitivity.txt` + `结果/log_statistical.txt` 全部存在且含正常结束标记。文件不全或无执行日志=本阶段无效，禁止进入阶段6.5。

> ⛔ **下一阶段预读锁（防忘机制）**：本阶段门禁通过后 → **必须立即 Read SKILL.md 中 阶段 6.5 的全部内容**（含 ⛔强制前置 + 🛠️工具调用 + 执行任务 + 产出 + 🛑门禁 + 检查点）。不预读 = 禁止开始 阶段 6.5。

---

### 阶段 6.5：对抗性验证

**执行指令**：
1. **三角色审稿人模拟（增强版，每位 5 问）**：方法论专家(A) + 实验设计专家(B) + 领域专家(C)，各提最尖锐的 5 个问题（包含新增的定理条件验证/可复现性挑战/实际应用压力测试等）。每个问题必须有 ≥3 句话实质性回答。详见 `references/workspace.md` "审稿人模拟深度增强"。
2. **极端情况测试**：参数取边界值(0/∞/负数) + 数据退化到简单情况 + 加 5%/20% 噪声。使用代码模板 `templates/extreme_test.py`。
3. **备选方案压力测试**：如果不用创新点 → 结果差多少？换一种模型设定 → 结论还成立？
4. **What-if 思维实验（新增）**：≥2 种系统性假设场景探索——数据规模变化（10%/50%/200%子采样学习曲线）+ 约束条件改变（±50%）+ 目标函数切换。明确标注"哪些结论是稳健的，哪些是条件依赖的"。详见 `references/workspace.md` "What-if 思维实验"。
5. 将所有问题和回答写入 `审稿意见与回复.md`，所有发现的问题必须修补

**产出**：`审稿意见与回复.md`（含 What-if 实验分析）

**⛔ 强制前置步骤（不执行则本阶段无效）**：先用 Read 工具读取 `references/workspace.md` 的对应章节（搜索"阶段 6.5"）后，才能开始本阶段。

**🛑 阶段门禁（文件+内容验证）**：完成后必须执行双重验证——① Glob `审稿意见与回复.md` 已创建；② Grep 文件中包含 `What-if` 或 `情景分析` 关键字。缺失=What-if实验未执行=本阶段无效，禁止进入阶段7。

> ⛔ **下一阶段预读锁（防忘机制）**：本阶段门禁通过后 → **必须立即 Read SKILL.md 中 阶段 7 的全部内容**（含 ⛔强制前置 + 🛠️工具调用 + 执行任务 + 产出 + 🛑门禁 + 检查点）。不预读 = 禁止开始 阶段 7。

---

### 阶段 7：代码审查

**执行指令**：审查 5 个维度——数值精度(浮点误差/条件数) + 收敛性 + 边界条件 + 可复现性(种子/确定性) + 计算效率(向量化/冗余)

**产出**：代码审查报告

**⛔ 强制前置步骤（不执行则本阶段无效）**：先用 Read 工具读取 `references/workspace.md` 的对应章节（搜索"阶段 7"）后，才能开始本阶段。

**🛑 阶段门禁（文件+溯源验证）**：完成后必须执行双重验证——① Glob `代码审查报告.md` 已创建；② Grep `代码/*.py` 中 `# \[数据驱动\]|\[理论推导\]|\[文献参考\]` 等 7 种溯源标签出现 ≥5 处。不足=代码注释溯源未执行=本阶段无效，禁止进入阶段8。

> ⛔ **下一阶段预读锁（防忘机制）**：本阶段门禁通过后 → **必须立即 Read SKILL.md 中 阶段 8 的全部内容**（含 ⛔强制前置 + 🛠️工具调用 + 执行任务 + 产出 + 🛑门禁 + 检查点）。不预读 = 禁止开始 阶段 8。

---

### 阶段 8：论文撰写

**执行指令**：
1. **8.0 写作前思考**（动笔前必须回答）：核心论点"金句"（含因果关系+反直觉转折+量化效果，模板"本文的核心发现是：...，这一发现修正了传统认知中的..."）→ 逻辑链条 → 创新点表达策略 → 评委可能问的 5 个最尖锐问题 → 与已有工作的本质区别。**金句必须贯穿全文**：Grep 验证核心短语在摘要/§2/§5/§9 中 ≥4 次。详见 `references/workspace.md` "核心论点"金句"贯穿机制"。
2. **8.1 论文结构**（按模板）：问题重述 → 问题分析(含技术路线图) → 模型假设(每条论证合理性) → 符号说明(三线表) → 逐问建模求解(理论分析+算法+消融+结果+不确定性) → 验证与灵敏度 → 讨论(机理解释三层+文献对比+局限) → 模型评价与推广 → 结论
3. **8.2 摘要写作**（论文生命线，**必须撑满整整一页**）：500-800 字 + 每问"针对问题X，"开头 + 每问精确数值 ≥5 位（含 95% CI）+ 至少 1 问交叉验证（两种方法+相对误差）+ 方法名高度具体（含算法全称和模型类型）+ 末尾含"本文主要贡献"声明段（≥3 条编号贡献）+ 金句嵌入+ 关键词 5 个分号分隔。页边距 2.5cm，摘要必须填满一页（末行距页面底部 ≤3cm），不得有大片空白。详见 `references/workspace.md` 摘要标准。
4. **8.3 学术化写作**：使用学术化中文，句式多样化，避免"首先其次此外最后"机械排列。**逐章遵循 9 大章节深度标准**（每章最低字数/段落数/引用数/图表数），详见 `references/workspace.md` "论文 9 大章节逐章深度标准"。
5. **8.4 逻辑自检**：每章核心论点→每句话支撑论点→段间逻辑→证据→无套话→预回答评委问题
6. **8.5 讨论深度**：与文献定量对比(≥3 篇) + 意外发现分析 + 局限诚实报告(来源→影响→合理性→消除路径) + 未来改进具体化
7. **8.6 文献综述**：总 ≥15 篇(≥5 英文) + 引用密度达到每段 2-4 处
8. **8.7 论文内容丰富度自检**：论文初稿完成后 → 对 14 项空洞内容标签逐一 Grep 扫描（如"具有良好的性能""在一定程度上""可以看出""首先其次最后"等）→ 全篇标签出现 ≤5 次 → 超过则回退逐段替换为具体内容。详见 `references/workspace.md` "论文整体防空洞内容机制"。
9. **8.8 模型对比总表（特等奖必备）**：论文 §6（验证与灵敏度）中必须包含一张"模型对比总表"——本文方法 vs ≥3 篇文献方法 vs ≥5 项指标（精度/拟合优度/计算效率/统计显著性/鲁棒性）的系统量化对比。本文方法至少 3 项指标最优。详见 `references/workspace.md` "模型对比总表要求"章节。
10. **8.9 MCM/ICM 英文摘要专项**（仅美赛）：按 6 要素标准结构（Problem/Approach/Per-Q/Validation/Contribution/Conclusion）写作，每问精确数值+CI，≥1问交叉验证，句长标准差>8词，避免中式英语。摘要≤1页。详见 `references/workspace.md` "MCM/ICM 英文摘要写作专项指南"章节。
11. **8.10 交叉引用密度检查（特等奖必备）**：4 项检查——孤儿/断链/密度/最大间隔。全篇 ≥40 处引用。产出"交叉引用密度检查报告"。
12. **8.11 问题间耦合衔接检查**：每问 §5.x 开头必须有"承上→启下→本问定位"三段式衔接段。Grep 验证 ≥3 处耦合关键词（"基于...结果""利用问题X的..."）。详见 `references/workspace.md` "问题间耦合衔接强制机制"。
13. **8.12 符号全局一致性检查**：提取全篇符号清单 → 4 项检查（唯一性/语义唯一性/命名规范/下标一致）→ 产出"符号一致性检查报告"。详见 `references/workspace.md` "符号定义全局一致性检查"。
14. **8.13 摘要首句冲击力**：首句必须使用矛盾型/反直觉型/空白型三种模板之一，≤50字，含具体数值。禁止 AI 高频开头。
15. **8.14 创新点索引表（特等奖必备）**：论文 §2 末尾（首选）或 §9 结论中，插入"创新点索引表"——编号+简述+类型+引入位置+理论依据+消融验证（含量化指标+显著性）+讨论位置+贡献度星级（⭐～⭐⭐⭐）。每个创新点的消融验证列必须非空且含量化指标。详见 `references/workspace.md` "创新点索引表"。
16. **8.15 三审三校论文质量迭代（特等奖关键——堵住论文级质量漏洞）**：论文初稿完成后，执行 3 轮审查 + 3 轮校对，每轮有独立产出和检查点。**强制工具链**：每轮 `Write` 产出审查报告 → `Glob` 验证报告存在 → 下轮才可开始。缺少任何一轮报告 = 阶段 8 门禁不通过。
    - **初审（逻辑结构）** → `一审报告.md`：检查 9 大章节逻辑连贯性、论证链条完整性、章节间衔接（"承上→启下"）、金句贯穿（Grep ≥4 次）。产出 ≥5 条具体修改意见。
    - **一校（格式规范）** → `一校报告.md`：2026 格式逐项核对（页边距 2.5cm、页码从摘要页起、无目录、无身份信息）、图表引用完整性、交叉引用无 "??"、参考文献格式 GB/T 7714。
    - **复审（内容深度）** → `二审报告.md`：逐章检查深度标准（每章最低字数/段落数/公式数/引用数，对照篇幅不足诊断表）、14 项空洞标签 Grep 扫描（≤5 次）、模型对比总表完备性、创新点索引表完整性。
    - **二校（语言表达）** → `二校报告.md`：句式多样化检查（Grep "首先.*其次.*最后" ≤1 次）、AI 率高危段落标记（"本文提出""具有良好的性能""实验结果表明"）、学术化中文复核、专业术语一致性。
    - **终审（整体一致性）** → `三审报告.md`：锁 7（结果溯源预检——抽样 5 个数值逐对核对）、锁 8（赛题响应度预检——逐条标注回应位置）、金句贯穿复核（Grep ≥4 次）、凌晨 2 点评委视角快速浏览（≥90 分）。
    - **三校（交叉引用+附录）** → `三校报告.md`：交叉引用密度 ≥40 处复核、附录代码可运行性预检、参考文献引用完整性（每篇至少引用 1 次）、支撑材料文件列表与论文附录一致性。
    - **三审三校门禁**：`Glob` 验证 6 份报告全部存在 + `Grep` 每份报告含 ≥3 条具体修改意见 + 所有报告的结论均为 `✅可进入下一阶段`。任一报告缺失或结论为 `❌需重新修改` → 回退阶段 8 重新修改 → 重新执行三审三校 → 直至全部通过。

**写作要求**：
- 正文 15000-25000 字（不含附录），正文 29-30 页
- 章节编号：中文数字主章节 + 阿拉伯数字子章节
- **插图尺寸**：单张 ≤3/4 页，优先两图并列（0.48-0.49\textwidth）最大化版面利用率，3D 图 0.8-0.9\textwidth（视觉冲击+两侧留白呼吸）。图表密度 0.6-0.9 张/页（冲刺档高信息密度）。连续图表页 ≤2 页（用文字段落穿插打破）。**禁止用大图撑页数**
- **必须使用 `templates/main.tex` LaTeX 模板**（符合 2026 官方规范）
- **必须包含模型对比总表**（§6 验证章节中，≥3篇文献×≥5项指标）
- **表格全部使用三线表**（booktabs，禁止竖线，表头含单位，最优值加粗，表注说明显著性标注）
- **附录代码含行号+语法高亮+listings 宏包**，按执行顺序排列，可直接复制运行
- **⛔ `\label{lastmain}` 必须存在**：在 `\section{结论}` 末尾插入 `\label{lastmain}`，紧接着是参考文献节（无需分页符），参考文献后 `\newpage` 分页再 `\appendix`。Label 标记正文最后一页（结论末）。锁 14 提取 P → N = P−1 页。不可删除。

**产出**：main.tex + references.bib

**⛔ 强制前置步骤（不执行则本阶段无效）**：先用 Read 工具读取 `references/workspace.md` 的对应章节（搜索"阶段 8"和"LaTeX 排版规范"和"论文 9 大章节逐章深度标准"和"论文整体防空洞内容机制"和"核心论点"金句"贯穿机制"和"问题间耦合衔接强制机制"和"符号定义全局一致性检查"和"论文第一句话冲击力要求"和"算法伪代码 LaTeX 排版规范"和"长公式排版规范"和"负面结果报告策略"和"创新点索引表"和"文本原创性处理"和"数学美学检查清单"和"图表在论文中的布局策略"和"图注(Caption)深度写作标准"和"模型对比总表要求"和"论文内部交叉引用密度检查"和"参考文献管理效率指南"和"降重检查清单"和"降 AI 率具体技巧"和"降重与降 AI 率的协同策略"和"MCM/ICM 英文摘要写作专项指南"）后，才能开始本阶段。

**🛑 阶段门禁（文件+内容+深度验证）**：完成后必须执行三重验证——① Glob `论文/main.tex` + `论文/references.bib`；② Grep main.tex ≥6/7 章节命令；③ Grep `阶段进度追踪.md` 中 `[CP6]` 标记存在且 ≥5 句，每句 ≥10 个中文字符，明确回答"删掉哪个创新点特等奖概率降幅最大？"且含 ≥1 处对论文具体章节/图表的编号引用。缺失或格式不达标=本阶段无效，禁止进入阶段9。

> ⛔ **下一阶段预读锁（防忘机制）**：本阶段门禁通过后 → **必须立即 Read SKILL.md 中 阶段 9 的全部内容**（含 ⛔强制前置 + 🛠️工具调用 + 执行任务 + 产出 + 🛑门禁 + 检查点）。不预读 = 禁止开始 阶段 9。

---

### 阶段 9：编译排版

**执行指令**：
```bash
cd 论文
xelatex main.tex
xelatex main.tex     # 第二次确保交叉引用正确
```

**生成 Word 版本**：`pandoc main.tex -o 论文终稿.docx`（备选：`python create_word.py`）

**LaTeX 常见问题**：xelatex 路径为 `C:\Users\rzy31\AppData\Local\Programs\MiKTeX\miktex\bin\x64\xelatex.exe`（MiKTeX 26.5），编译前需 `$env:Path = "C:\Users\rzy31\AppData\Local\Programs\MiKTeX\miktex\bin\x64;$env:Path"`；ctex 未安装 → `tlmgr install ctex`；字体缺失 → 改 `\setCJKmainfont`；Overfull hbox → 调列宽或字号

**产出**：main.pdf + 论文终稿.docx

**⛔ 强制前置步骤（不执行则本阶段无效）**：先用 Read 工具读取 `references/workspace.md` 的对应章节（搜索"阶段 9"和"论文插图尺寸与紧凑性强制标准"和"附录完整性验证"和"LaTeX 浮动体与页面优化策略"和"竞赛特有 LaTeX 模板差异适配"和"紧急情况处置预案"和"论文篇幅锁"）后，才能开始本阶段。

**🛑 阶段门禁（文件+篇幅+附录+防撑页数，锁 14）**：完成后必须执行四重验证——

① **Glob 文件验证**：`论文/main.pdf` 已创建且文件大小 > 100KB（29-30页正文通常>500KB）+ `论文/main.aux` 存在 + **页数快速验证**：Grep PDF 页数 ≥8 页（xelatex 编译日志或 Read PDF metadata）+ **摘要撑满一页验证**：打开 PDF 检查摘要页末行距页面底部 ≤3cm（约 5-6 行空白以内）。空白过大 → 摘要内容不足 → 回退阶段 8 扩充摘要。

② **📏 正文篇幅验证（锁 14 核心 — Label 精确定位法）**：
  - **前提**：`main.tex` 中 `\section{结论}` 末尾必须有 `\label{lastmain}`，紧接着是参考文献节（无需分页符），参考文献结束后 `\newpage` 分页再 `\appendix`。编译前 Grep 验证 `\label{lastmain}` 存在于结论节中 + 参考文献后 `\newpage` 存在。
  - **方法**：Grep `main.aux` 中 `\newlabel{lastmain}` → 提取 P → **正文 N = P−1**（P 含摘要第1页）
  - 正文 = 第2页(问题重述)→第P页(结论末)。参考文献从 P+1 页起。附录从参考文献后的 `\newpage` 起，页数无上限。
  - **N < 25 → 🔴 严重不足** → 回退阶段 5/1
  - **25 ≤ N < 29 → 🟡 深度不足** → 回退阶段 8，诊断表定位薄弱章节 → 精准扩充 → 重新编译
  - **N = 29-30 → 🟢 达标** → 进入防撑页数检查
  - **N > 30 → 🟠 超标** → 精简正文（压缩图表/移次要推导至附录）→ 重新编译

③ **附录完整性验证**：
  - Glob 附录中 .py 文件数 ≥ 8（utils + data_preprocessing + stage4_eda + 全部 problemN + sensitivity + statistical + extreme_test + verify）
  - Glob README.md + requirements.txt 存在
  - 附录页数 ÷ 正文页数 ≤ 60%
  - 文件总大小 ≤ 20MB

④ **📐 防插图撑页数检查**（N 在 29-32 即 P 在 30-33 时触发）：
  - Grep `\includegraphics[width=` 中 `\textwidth` 的比例
  - ≥30% 全宽图 → 缩小至 0.7-0.85\textwidth → 释放空间用文字补充 → 重新编译
  - 重新编译后 **N 在 29-30（P 在 30-31）** → ✅ 通过（回到目标范围）
  - 重新编译后 **N > 30（P ≥ 32）** → 仍超标 → 继续精简正文（压缩图表/移次要推导至附录）
  - 重新编译后 **N < 29（P < 30）** → 原页码依赖大图 → 回退阶段 8

**回退循环控制**：最多 3 次。第 3 次仍 < 30 页 → 触发系统诊断（赛题理解偏差？/ 模型过于简化？/ 赛题规模确实小？）→ 论文 §7 诚实说明。全部通过 → 阶段有效 → 进入阶段 10。

> ⛔ **下一阶段预读锁（防忘机制）**：本阶段门禁通过后 → **必须立即 Read SKILL.md 中 阶段 10 的全部内容**（含 ⛔强制前置 + 🛠️工具调用 + 执行任务 + 产出 + 🛑门禁 + 检查点）。不预读 = 禁止开始 阶段 10。

---

### 阶段 10：最终检查

**执行指令**：
0. **🔒 最终重跑验证（阶段 10 首位强制执行）**：清空所有结果缓存（删除 `结果/` 下所有 CSV/图表），从头重新运行全部脚本：`data_preprocessing.py → stage4_eda.py → problemN.py → sensitivity_analysis.py → statistical_tests.py`。用最新运行输出对比论文数值，确保所有结果是可复现的最新版本。任何不一致 → 修正论文或代码 → 重新重跑。
1. **5 类一致性验证**（强制执行）：数值结果(逐条核对论文 vs 代码输出) → 图表数据(每张图在代码中找到生成脚本) → 代码完整性(附录代码可直接运行) → 公式一致性(每个编号公式有代码对应) → 参考文献可追溯性(每个 [N] 有对应条目)。产出 `consistency_report.txt`。
2. **🔒 赛题响应度检查表**（强制执行，对应第 8 道锁）：将赛题原文逐条拆分为所有显性+隐含要求 → 逐条标注论文中的回应位置（页码/行号/图表编号）→ 产出 `赛题响应度检查表.md`。任何一条无回应 → 必须回退补充论文 → 重新检查，直至全部有回应。
3. **🔗 交叉引用密度检查**（强制执行，全篇 ≥40 处引用）：4 项检查——(1)孤儿检查（每张图/表/公式在正文中至少被引用 1 次）；(2)断链检查（所有 \ref/\cite 指向存在的 label）；(3)密度检查（全篇 ≥40 处，每章节达到最低标准）；(4)最大无引用间隔检查（≤2 页）。产出"交叉引用密度检查报告"。详见 `references/workspace.md` "论文内部交叉引用密度检查"章节。
4. **🔍 论文内容丰富度扫描（强制执行，防空洞内容）**：对论文 final PDF/TeX 执行 14 项空洞标签 Grep 扫描——`具有良好的性能`、`在一定程度上`、`可以看出`、`首先.*其次.*最后`、`具有重要意义`、`有待进一步`、重复句式、`该模型`连续使用、`基于.*的`连续开头、因果连接词连续使用、`通过.*对.*进行了`、`有效的\|可行的\|合理的`作为独立结论、`本文提出了`、连续3段相同词性开头。全篇标签出现 ≤5 次 → 通过。超过 5 次 → 论文内容空洞 → 回退逐段替换为具体内容 → 重新扫描。产出 `内容丰富度扫描报告.txt`。详见 `references/workspace.md` "论文整体防空洞内容机制"。
5. **格式合规检查**（2026 官方规范）：正文 ≤30 页 + 无目录 + 电子版首页为摘要页 + 页码从摘要页起 + 无身份信息 + 附录含完整代码（按 listings 宏包格式：行号+语法高亮+按执行顺序排列）+ 文件 ≤20MB
6. **📎 提交文件命名与匿名化检查**：根据赛事类型逐项核对提交规范——国赛检查（选题编号+队号命名/不得含学校信息/承诺书/≤20MB）、美赛检查（队号命名/Summary Sheet 首页/Control Sheet/完全匿名/≤20MB）。详见 `references/workspace.md` "提交文件命名与匿名化竞赛适配检查清单"。
7. **📄 环境复现说明生成**：在支撑材料中生成 `README.md`——含 Python 版本+依赖安装+完整运行步骤+预期运行时间+关键输出与论文位置对应表。验收标准：按 README 步骤从头运行，所有脚本无报错且数值一致。
8. **🕐 凌晨2点评委视角终读模拟**（提交通行证）：角色扮演疲惫评委（凌晨2:17/已评47份/5分钟浏览）→ 按 5 分钟快速浏览路径（标题→摘要→图表→建模→验证→参考文献）→ 回答 5 个问题（核心贡献/最弱环节/特等奖投票/改进建议/致命缺陷）→ 自评 ≥90 分+无致命缺陷=可提交。产出 `终读模拟报告.md`。详见 `references/workspace.md` "凌晨 2 点评委视角终读模拟"。
9. **论文质量量化自评**：6 维度 × 5 子项 = 30 项 0-5 分评分（总分 ≥95 且各维度 ≥90%）
10. **10 项特等奖门禁**：全部通过方可提交
11. **支撑材料打包**：生成 `支撑材料.zip`（含全部代码 + 中间结果 + README.md + 自主查阅数据 + 图表），验证 ≤20MB，验证文件列表与论文附录一致。Glob 确认 `.zip` 存在。
12. **🔒 附录代码可运行性独立验证（锁16，强制执行）**：(1)从 `main.tex` 附录中提取全部 `.py` 代码 → 写入 `代码/附录提取/`；(2)清空 `结果/` 缓存 → 从头运行提取的代码；(3)对比运行输出与论文中的全部数值（≥4 位有效数字逐对核对）；(4)任一不一致 → 修正论文或代码 → 重新提取 → 重新运行 → 重新核对 → 直至全部一致。产出 `附录代码可运行性验证报告.md`。
13. **📦 提交包完整性检查（锁15，强制执行）**：(1)论文 PDF 命名正确（国赛：选题编号+队号.pdf；美赛：队号.pdf）；(2)支撑材料命名与格式合规（国赛：RAR/ZIP；美赛：ZIP；≤20MB）；(3)论文无身份信息（Grep 中文姓名/学校/赛区）；(4)MD5 校验值已记录。逐项标记 ✅/❌。任一 ❌ → 立即修复。产出 `提交包完整性检查表.md`。

**产出**：提交检查表 + consistency_report.txt + 赛题响应度检查表.md + 交叉引用密度检查报告 + 内容丰富度扫描报告.txt + 终读模拟报告.md + README.md + 附录代码可运行性验证报告.md + 提交包完整性检查表.md + 支撑材料.zip

**⛔ 强制前置步骤（不执行则本阶段无效）**：先用 Read 工具读取 `references/workspace.md` 的对应章节（搜索"阶段 10"和"代码-论文一致性验证协议"和"论文内部交叉引用密度检查"和"论文整体防空洞内容机制"和"提交文件命名与匿名化竞赛适配检查清单"和"凌晨 2 点评委视角终读模拟"和"各阶段紧急时间预案"和"论文质量量化自评表"和"特等奖强制门禁"）后，才能开始本阶段。

**🛑 阶段门禁（8文件全验证）**：完成后必须用 Glob 验证以下 **8 项全部**存在且通过：(1) `consistency_report.txt`（5类全通过）；(2) `赛题响应度检查表.md`（全部条目有回应）；(3) `交叉引用密度检查报告`（4项全通过，全篇≥40处引用）；(4) `内容丰富度扫描报告.txt`（14标签扫描≤5次）；(5) `终读模拟报告.md`（自评≥90分+无致命缺陷）；(6) `附录代码可运行性验证报告.md`（锁16，全部数值一致）；(7) `提交包完整性检查表.md`（锁15，4/4 项 ✅）；(8) `支撑材料.zip`（含README.md，≤20MB）。任一缺失或未通过 = **禁止提交**，必须回退修复后重新验证。

> ⛔ **下一阶段预读锁（防忘机制）**：本阶段门禁通过后 → **必须立即 Read SKILL.md 中 阶段 11 的全部内容**（含 ⛔强制前置 + 🛠️工具调用 + 执行任务 + 产出 + 🛑门禁 + 检查点）。不预读 = 禁止开始 阶段 11。

---

### 阶段 11：答辩准备（如需）

**执行指令**：
1. **PPT 结构**(6-8 页)：背景与问题 → 技术路线图 → 每问核心方法+关键结果 → 创新点与消融 → 验证与灵敏度 → 结论与推广
2. **10 个预答问题**（覆盖评委提问 90%）：方法选择/创新辩护/假设质疑/参数辩护/过拟合排除/灵敏度/推广性/局限性/实际价值/时间管理
3. **演练**：≥2 次完整计时演练 + 1 次刁钻问答模拟 + 1 次极端压力模拟

**产出**：答辩PPT.pptx

**⛔ 强制前置步骤（不执行则本阶段无效）**：先用 Read 工具读取 `references/workspace.md` 的对应章节（搜索"阶段 11"）后，才能开始本阶段。

**🛑 阶段门禁（文件验证）**：完成后必须用 Glob 验证 `答辩PPT.pptx` 已创建。

---

## 📋 全流程阶段速查表（快速索引）

| 阶段 | 核心动作 | workspace.md 章节 | 代码模板 |
|------|---------|------------------|---------|
| 0 | 五维选题评估 + 计算预判 | "阶段 0" + "竞赛适配器" | — |
| 1 | 三层追问 + 跨领域迁移 + 统一框架 | "阶段 1" (1.1-1.8) | — |
| 2 | 时间分配 + 里程碑 + 风险预案 | "阶段 2" | — |
| 3 | pip install + 目录结构 | "阶段 3" | requirements.txt |
| 4 | 预处理管线 + EDA + 假设链 + 参数CI | "阶段 4" (4.0-4.4) | utils.py |
| 5 | 建模→理论→算法→自批判→消融→结果→不确定性→机理 | "阶段 5" (5.0-5.8) | problem_template.py |
| 6 | 交叉验证+统计检验+Sobol+MC+假设松弛+Pareto | "阶段 6" + 5.0.2 | sensitivity_analysis.py, statistical_tests.py |
| 6.5 | 三角色审稿 + 极端测试 + What-if | "阶段 6.5" | — |
| 7 | 精度/收敛/边界/复现/效率 | "阶段 7" | — |
| 8 | 论文撰写（金句→结构→摘要→文献→自检→对比→交叉引用→创新索引→三审三校） | "阶段 8" (8.0-8.15) | main.tex |
| 9 | xelatex×4 + 锁14篇幅验证 + 附录完整性 + 防撑页 | "阶段 9" | — |
| 10 | 重跑+一致性+赛题响应度+交叉引用+防空洞+终读+附录验证(锁16)+提交完整性(锁15)+支撑材料 | "阶段 10" | verify_consistency.py |
| 11 | PPT + Q&A + 演练 | "阶段 11" | — |

---

## 🛠️ 各阶段工具调用位置（AI 必须按此表执行，不可省略）

> **核心作用**：本表定义每个阶段**必须调用的工具、命令和调用位置**。AI 在执行每个阶段时，必须严格按照此表调用工具。**工具调用不可省略、不可替换、不可跳过**。

### 工具调用总表

| 阶段 | 调用位置 | 工具 | 命令 / 操作 | 产出 / 验证 |
|------|---------|------|------------|------------|
| **0** | 阶段开始 | **Read** | `Read references/workspace.md` 搜索"阶段 0"+"竞赛适配器" | ⛔强制前置 |
| 0 | 阶段结束 | **Write** | `Write 选题决策记录.md` | 选题决策记录.md |
| 0 | 阶段门禁 | **Glob** + **Grep** | Glob `选题决策记录.md` + Grep 4关键字（选题评估表+计算预判+歧义决策+附件文件清单） | 🛑文件+内容 |
| **1** | 阶段开始 | **Read** | `Read references/workspace.md` 搜索"阶段 1"+"跨领域"+"反套路"+"标题设计" | ⛔强制前置 |
| 1 | 阶段结束 | **Write** | `Write 赛题分析.md` | 赛题分析.md |
| 1 | 阶段门禁 | **Glob** + **Grep** | Glob `赛题分析.md` + Grep ≥8/10关键字 + Grep [CP1][CP2]（≥5句 ≥10字/句 + 原文引用 + 数值） | 🛑文件+内容+深度 |
| **2** | 阶段开始 | **Read** | `Read references/workspace.md` 搜索"阶段 2"+"团队协作" | ⛔强制前置 |
| 2 | 阶段结束 | **Write** | `Write 建模计划.md` | 建模计划.md |
| 2 | 阶段门禁 | **Glob** + **Grep** | Glob `建模计划.md` + Grep 3关键字（时间分配+里程碑+风险预案） | 🛑文件+内容 |
| **3** | 阶段开始 | **Read** | `Read references/workspace.md` 搜索"阶段 3"+"代码架构"+"版本控制" | ⛔强制前置 |
| 3 | 创建依赖文件 | **Write** | `Write 代码/requirements.txt`（内容参考 templates/requirements.txt） | requirements.txt |
| 3 | 创建目录结构 | **PowerShell** | `New-Item -ItemType Directory -Force 代码, 结果/figures, 论文, 答辩` | 标准目录 |
| 3 | 安装依赖 | **PowerShell** | `pip install -r 代码/requirements.txt` | 全部包可导入 |
| 3 | 安装验证 | **PowerShell** | `python -c "import numpy,pandas,matplotlib,scipy,sklearn,statsmodels,seaborn,networkx,openpyxl; print('OK')"` | 依赖就绪 |
| 3 | 阶段门禁 | **Glob** | Glob `代码/requirements.txt` + 验证 `代码/` `结果/figures/` `论文/` `答辩/` 目录存在 | 🛑文件验证 |
| **4** | 阶段开始 | **Read** | `Read references/workspace.md` 搜索"阶段 4"+"4.0"+"预处理管线" | ⛔强制前置 |
| 4 | 预处理脚本 | **Write** | `Write 代码/data_preprocessing.py`（参考模板 `templates/data_preprocessing.py`） | data_preprocessing.py |
| 4 | 预处理管线 | **PowerShell** | `python 代码/data_preprocessing.py 2>&1 | Tee-Object -FilePath "结果/log_data_preprocessing.txt"` | data_manifest.json + preprocessed_data.csv + preprocessing_log.txt + **执行日志** |
| 4 | EDA 脚本 | **Write** | `Write 代码/stage4_eda.py`（参考模板 `templates/stage4_eda.py`） | stage4_eda.py |
| 4 | EDA 分析 | **PowerShell** | `python 代码/stage4_eda.py 2>&1 | Tee-Object -FilePath "结果/log_stage4_eda.txt"` | data_quality_report.md + EDA 图表 ≥3 张 + **执行日志** |
| 4 | 洞察报告 | **Write** | `Write 数据洞察报告.md` | 数据洞察报告.md |
| 4 | 阶段门禁 | **Glob** + **Grep** | Glob 6文件（data_manifest/preprocessed_data/preprocessing_log/data_quality_report/数据洞察报告/figD≥3张）+ Grep [CP3]（≥5句 ≥10字/句 + 原文引用 + ≥2数值） | 🛑文件+内容+深度 |
| **5** | 阶段开始 | **Read** | `Read references/workspace.md` 搜索"阶段 5"+"5.0"+"5.0.1"+"5.0.2"+"ODE求解"+"问题脚本" | ⛔强制前置 |
| 5 | 问题1求解 | **PowerShell** | `python 代码/problem1.py 2>&1 | Tee-Object -FilePath "结果/log_problem1.txt"` | 结果图 + CSV + **执行日志** |
| 5 | 问题1门禁 | **Glob** | 验证 problem1.py 的产出图（≥2张）+ CSV（≥1个）+ log_problem1.txt（含✅）存在 | ⛔逐问锁 |
| 5 | 问题2求解 | **PowerShell** | `python 代码/problem2.py 2>&1 | Tee-Object -FilePath "结果/log_problem2.txt"`（问题1门禁通过后） | 结果图 + CSV + **执行日志** |
| 5 | 问题3求解 | **PowerShell** | `python 代码/problem3.py 2>&1 | Tee-Object -FilePath "结果/log_problem3.txt"`（如有第3问） | 结果图 + CSV + **执行日志** |
| 5 | 问题4求解 | **PowerShell** | `python 代码/problem4.py 2>&1 | Tee-Object -FilePath "结果/log_problem4.txt"`（如有第4问） | 结果图 + CSV + **执行日志** |
| 5 | 3D 图数量验证 | **Glob** | `Glob "结果/figures/*3d*.png"` → 统计 3D 图数量 ≥12 张（问题分析≥2+每问≥2×N+验证≥3+对比≥2+结论≥1，每张含双视角） | 锁9-3D子约束 |
| 5 | 阶段门禁 | **Glob** + **Grep** | Glob 全部problemN.py + 图表≥22(含≥12张3D) + CSV + Grep ≥7/8关键字 + 锁11子步骤验证 + Grep [CP4][CP5] | 🛑文件+内容+3D+子步骤 |
| **6** | 阶段开始 | **Read** | `Read references/workspace.md` 搜索"阶段 6"+"5.0.2"+"统计显著性"+"统计检验"+"假设松弛"+"Pareto" | ⛔强制前置 |
| 6 | 灵敏度分析 | **PowerShell** | `python 代码/sensitivity_analysis.py 2>&1 | Tee-Object -FilePath "结果/log_sensitivity.txt"` | 灵敏度图≥5 + CSV + **执行日志** |
| 6 | 统计检验 | **PowerShell** | `python 代码/statistical_tests.py 2>&1 | Tee-Object -FilePath "结果/log_statistical.txt"` | 统计检验报告 + **执行日志** |
| 6 | 阶段门禁 | **Glob** | Glob ≥5 张灵敏度图（Tornado+Sobol1st+SobolTotal+MC+假设松弛+Pareto）+ 统计报告 | 🛑文件验证 |
| **6.5** | 阶段开始 | **Read** | `Read references/workspace.md` 搜索"阶段 6.5" | ⛔强制前置 |
| 6.5 | 极端测试 | **PowerShell** | `python 代码/extreme_test.py 2>&1 | Tee-Object -FilePath "结果/log_extreme_test.txt"`（必须执行） | 极端测试结果 + **执行日志** |
| 6.5 | 审稿回复 | **Write** | `Write 审稿意见与回复.md` | 审稿意见与回复.md |
| 6.5 | 阶段门禁 | **Glob** + **Grep** | Glob `审稿意见与回复.md` + Grep `What-if|情景分析` | 🛑文件+内容 |
| **7** | 阶段开始 | **Read** | `Read references/workspace.md` 搜索"阶段 7" | ⛔强制前置 |
| 7 | 代码检查 | **PowerShell** | 逐个运行全部 .py（data_preprocessing→stage4_eda→problemN→sensitivity→statistical→extreme_test→verify_consistency），每个脚本追加 `2>&1 \| Tee-Object -FilePath "结果/log_stage7_rerun.txt" -Append` | 所有脚本可运行 + **重跑日志** |
| 7 | 审查报告 | **Write** | `Write 代码审查报告.md` | 代码审查报告.md |
| 7 | 溯源验证 | **Grep** | Grep `代码/*.py` 中 7 种溯源标签 ≥5 处 | 🛑代码注释溯源 |
| 7 | 阶段门禁 | **Glob** + **Grep** | Glob `代码审查报告.md` + Grep 溯源标签 ≥5 处 | 🛑文件+溯源 |
| **8** | 阶段开始 | **Read** | `Read references/workspace.md` 搜索"阶段 8"+"LaTeX排版"+"文本原创性"+"数学美学"+"图表布局"+"图注(Caption)深度写作"+"模型对比总表"+"交叉引用密度"+"降重"+"降AI率"+"MCM/ICM 英文摘要" | ⛔强制前置 |
| 8 | 论文 LaTeX | **Write** | `Write 论文/main.tex`（使用 templates/main.tex 模板） | main.tex |
| 8 | 参考文献 | **Write** | `Write 论文/references.bib` | references.bib |
| 8 | 阶段门禁 | **Glob** + **Grep** | Glob `论文/main.tex` + `论文/references.bib` + Grep ≥6/7关键章节（问题重述/模型假设/符号说明/模型建立与求解/验证与灵敏度/模型评价与推广/参考文献）+ Grep [CP6]（≥5句 ≥10字/句 + 章节图表编号引用） | 🛑文件+内容+深度 |
| **9** | 阶段开始 | **Read** | `Read references/workspace.md` 搜索"阶段 9"+"紧急情况"+"篇幅锁"+"插图尺寸"+"附录完整性" | ⛔强制前置 |
| 9 | PATH设置 | **PowerShell** | `$env:Path = "C:\Users\rzy31\AppData\Local\Programs\MiKTeX\miktex\bin\x64;$env:Path"` | xelatex 可用 |
| 9 | LaTeX编译 | **PowerShell** | `cd 论文; xelatex main.tex; bibtex main; xelatex main.tex; xelatex main.tex`（编译4步确保交叉引用+BibTeX正确） | main.pdf |
| 9 | Word生成 | **PowerShell** | `pandoc 论文/main.tex -o 论文/论文终稿.docx --from=latex --to=docx --standalone --number-sections`（备选：`python 论文/create_word.py`） | 论文终稿.docx |
| 9 | 编译失败应急 | **PowerShell** | 若 xelatex 失败 ≥3 次 → `pandoc main.tex -o 论文终稿.docx --standalone --number-sections` 确保有可提交文件 | 应急产出 |
| 9 | 篇幅验证 | **Grep** | Grep `main.aux` 中 `\newlabel{lastmain}` → 提取 P → 正文 N=P−1 → 按锁14判定表处理（N<25→回退5/1; 25≤N<29→回退8; N=29-30→防撑页检查; N>30→精简） | 🛑篇幅锁14 |
| 9 | 阶段门禁 | **Glob** + **Grep** | Glob `main.pdf`(>100KB) + `main.aux` + **Grep N=29-30页** + **摘要撑满一页** + 附录完整性(≥8.py+README+附录/正文≤60%+≤20MB) + 防撑页检查 | 🛑篇幅+附录+摘要 |
| **10** | 阶段开始 | **Read** | `Read references/workspace.md` 搜索"阶段 10"+"一致性验证"+"交叉引用密度"+"防空洞内容"+"质量自评"+"特等奖门禁"+"终读模拟"+"提交命名" | ⛔强制前置 |
| 10 | 最终重跑 | **PowerShell** | 清空结果缓存 → 从头重跑全部脚本（每个脚本 `2>&1 \| Tee-Object -FilePath "结果/log_rerun_*.txt"`）→ 对比论文数值 | 可复现性验证 + **全部重跑日志** |
| 10 | 一致性验证 | **PowerShell** | `python 代码/verify_consistency.py 2>&1 | Tee-Object -FilePath "结果/log_consistency.txt"` | consistency_report.txt（5类全通过）+ **执行日志** |
| 10 | 赛题响应度 | **Write** | 逐条拆分赛题要求 → 逐条标注论文回应位置 → `Write 赛题响应度检查表.md` | 赛题响应度检查表.md（全部条目有回应） |
| 10 | 交叉引用检查 | **Grep** | 4项检查：孤儿/断链/密度(≥40处)/最大间隔(≤2页) → 产出交叉引用密度检查报告 | 交叉引用密度检查报告 |
| 10 | 内容丰富度扫描 | **Grep** | 14项空洞标签 Grep 扫描 → 全篇 ≤5 次 → 产出内容丰富度扫描报告.txt | 内容丰富度扫描报告.txt（≤5次） |
| 10 | 终读模拟 | **Write** | 凌晨2点评委视角5分钟快速浏览 → 自评≥90分+无致命缺陷 → `Write 终读模拟报告.md` | 终读模拟报告.md（≥90分） |
| 10 | 附录代码验证 | **PowerShell** | 从 main.tex 附录提取全部 .py → 独立运行 → 对比论文数值 → `Write 附录代码可运行性验证报告.md` | 附录代码可运行性验证报告.md（锁16，全部一致） |
| 10 | 提交完整性检查 | **Write** + **Grep** | 逐项核对（命名/格式/匿名/MD5）→ `Write 提交包完整性检查表.md` → Grep 4/4 ✅ | 提交包完整性检查表.md（锁15，4/4 ✅） |
| 10 | 支撑材料打包 | **PowerShell** | 生成 `支撑材料.zip`（全部代码+中间结果+README.md+图表）→ 验证 ≤20MB | 支撑材料.zip（≤20MB） |
| 10 | 阶段门禁 | **Glob** | 验证全部8项：consistency_report.txt（5类全通过）+ 赛题响应度检查表.md（全部有回应）+ 交叉引用密度检查报告（4项全通过≥40处）+ 内容丰富度扫描报告.txt（≤5次）+ 终读模拟报告.md（≥90分无致命缺陷）+ 附录代码可运行性验证报告.md（锁16，全部数值一致）+ 提交包完整性检查表.md（锁15，4/4 ✅）+ 支撑材料.zip（≤20MB含README.md） | 🛑提交就绪 |
| **11** | 阶段开始 | **Read** | `Read references/workspace.md` 搜索"阶段 11" | ⛔强制前置（如需） |
| 11 | PPT生成 | **PowerShell** | `python 代码/create_ppt.py`（参考模板 `templates/create_ppt.py`） | 答辩PPT.pptx |
| 11 | 阶段门禁 | **Glob** | `Glob "答辩PPT.pptx"` | 🛑文件验证 |

### 关键工具路径速查

> **优先级**：短命令（PATH 已设置时可用）→ 完整路径（PATH 不可用时的备选）。AI 优先尝试短命令；若报"未找到"，立即切换完整路径。

| 工具 | 短命令 | 完整路径 | 说明 |
|------|--------|---------|------|
| **Python 3.13** | `python` | `C:\Users\rzy31\AppData\Local\Programs\Python\Python313\python.exe` | 所有 `.py` 脚本入口 |
| **pip** | `pip` | `C:\Users\rzy31\AppData\Local\Programs\Python\Python313\Scripts\pip.exe` | Python 包管理 |
| **xelatex** | `xelatex` | `C:\Users\rzy31\AppData\Local\Programs\MiKTeX\miktex\bin\x64\xelatex.exe` | MiKTeX 26.5，编译前需 `$env:Path = "C:\Users\rzy31\AppData\Local\Programs\MiKTeX\miktex\bin\x64;$env:Path"` |
| **pandoc 3.10** | `pandoc` | `C:\Users\rzy31\AppData\Local\Microsoft\WinGet\Packages\JohnMacFarlane.Pandoc_Microsoft.Winget.Source_8wekyb3d8bbwe\pandoc-3.10\pandoc.exe` | LaTeX→docx 转换 |
| **Read 工具** | Read | — | 读取 workspace.md 指定章节（⛔强制前置） |
| **Write 工具** | Write | — | 创建产出文件 |
| **Edit 工具** | Edit | — | 修改已有文件 |
| **Glob 工具** | Glob | — | 验证文件存在（🛑阶段门禁） |
| **Grep 工具** | Grep | — | 搜索文件内容 + **验证阶段产出文件的内容深度**（🛑内容门禁） |
| **PowerShell 工具** | PowerShell | — | 执行命令行操作（所有 shell 命令入口） |

### 工具调用强制规则

1. **⛔ Read 物理门禁**：每个阶段开始前，必须先用 Read 读取 `references/workspace.md` 的指定章节。Read 未执行 = 本阶段无效。Read 完成后在 `阶段进度追踪.md` 记录行号范围（锁6物理强制）。
2. **🔧 PowerShell 可执行操作**：`pip install`、`python 脚本`、`xelatex 编译`、`pandoc 转换` 必须通过 PowerShell 工具实际执行。**禁止仅生成代码而不运行**。
3. **🛑 Glob 防伪造**：每个阶段结束后 Glob 验证产出文件。**Glob 输出文件列表必须完整粘贴到 `阶段进度追踪.md` 中**（含文件名+大小），不可仅写"全部通过"四字。无粘贴 = 门禁无效。
4. **✍️ Write 产出确认**：产出文件通过 Write 创建后，立即 Glob 验证写入成功。
5. **🐍 Python 执行日志强制留存**：阶段 4-10 每个脚本运行时追加 `2>&1 | Tee-Object -FilePath "结果/log_xxx.txt"`， stdout+stderr 保存到 `结果/log_*.txt`。Glob 验证日志存在且含正常结束标记（`✅`或`完成`）。无日志或无结束标记 = 脚本未实际运行 = 禁止通过门禁。
6. **📄 xelatex 编译 ≥2 次**：第一次生成 .aux，第二次解决交叉引用。编译 `.log` 文件需 Glob 验证存在。失败 ≥3 次 → pandoc Word。
7. **🔍 Grep 防伪造**：Grep 结果必须完整粘贴到 `阶段进度追踪.md`（含匹配行内容），不可仅写"≥7/8 通过"六字。无粘贴 = 门禁无效。

---

## ⏱️ 深度-广度动态平衡规则

原则 1（深度优先）与全流程覆盖之间存在天然矛盾。以下规则定义何时深挖、何时快速通过：

| 问题类型 | 深度策略 | 时间上限 |
|---------|---------|---------|
| 核心创新点 | 深挖到底 | 无硬上限（能持续产生洞见） |
| 理论分析 | 至少完成 2 项 | 每项 ≤2h |
| 数据洞察 | 至少 3 条假设链 | ≤4h |
| 灵敏度分析 | Sobol + Tornado | ≤3h |
| 论文排版 | 格式合规即可 | ≤2h |
| 答辩 PPT | 6-8 页 | ≤3h |

**必须深挖的铁律**（不可快速跳过）：
1. 数据与预期严重不符 → 可能是隐藏的创新点
2. 两个模型结论互相矛盾 → 可能是理论发现
3. 模型呈现相变/临界/非线性响应 → 需要机理解释
4. 审稿人模拟中的问题无法用现有材料回答

**迭代截止条件**（满足任一即停止）：
- 收敛截止：连续 2 轮改进后核心指标提升 < 5% 且 ≥ baseline × 1.5
- 时间截止：当前阶段用时 > 分配时间的 150% 且已满足 P0 要求
- 质量截止：自评表各维度 ≥ 85% 且总分 ≥ 90
- 降级截止：2 轮后仍无法找到权威定理支撑理论深度 → 降级为一等A档

---

## 🔬 创新产生机制（系统性方法）

### 跨领域方法迁移矩阵
对每个问题，从以下领域系统性排查可迁移方法：

| 来源领域 | 典型方法 | 核心类比问题 |
|---------|---------|------------|
| 物理学 | 统计力学、Ising 模型、渗透理论 | 网络传播/相变/临界行为 |
| 生态学 | Lotka-Volterra、种群动力学 | 竞争/捕食/共存系统 |
| 经济学 | 拍卖理论、机制设计、匹配市场 | 资源分配/博弈/定价 |
| 流行病学 | SIR/SEIR、分支过程 | 信息传播/级联/扩散 |
| 运筹学 | 排队论、库存论、调度理论 | 等待/存储/排序问题 |
| 控制论 | PID、MPC、最优控制 | 调控/优化/反馈系统 |
| 信息论 | 熵、互信息、率失真 | 不确定性量化/特征选择 |
| 博弈论 | 演化博弈、纳什均衡 | 多方决策/策略互动 |

### 标准方法系统性批判
对每问的"教科书式解法"必须回答：
1. 核心假设是什么？
2. 在本题中是否成立？（用数据验证）
3. 如果不成立，会导致什么偏差？
4. 如何修正？→ 这就是创新点

### 创新点产出格式
每个创新点必须包含：来源（跨领域/标准方法修正/数据洞察）、核心思想（一句话）、理论依据、实现路径、预期效果（量化）、验证方法（消融实验）、失败预案（备选方案）。

---

## 📊 关键质量标准

### 摘要（论文生命线——必须撑满整整一页）

**结构**（从上到下，填满一页）：
1. 标题（小二号黑体居中）
2. "摘  要"（四号黑体居中）
3. 摘要正文 500-800 字，含以下段落：
   - 开头段（2-4句）：问题背景 + 一句话整体方法概述 + **金句嵌入**
   - "针对问题一，"段（3-5句）：方法全称 + 推导简述 + 精确数值 ≥5位 + 95%CI + 交叉验证（可选）
   - "针对问题二，"段（同上）
   - ...（每问一段）
   - 贡献段（3-5句）："本文的主要贡献在于：(1)...(2)...(3)..." + 灵敏度/验证结论
4. 关键词行：5 个关键词，分号分隔

**硬性标准**：
- 字数 500-800 字（确保撑满一页，12pt，1.25 倍行距，2.5cm 页边距）
- 每问精确数值 ≥5 位有效数字 + 95% CI
- 至少 1 问含交叉验证（两种方法 + 相对误差百分比）
- 至少 1 处统计指标（R²/AIC/p 值/RMSE）
- 方法名称高度具体（含算法全称和模型类型，如"基于 Kuhn-Tucker 条件的非凸优化算法"而非"优化算法"）
- 关键词 5 个，分号分隔，含核心方法名
- **撑满一页验证**（阶段 9）：摘要末行距页面底部 ≤3cm（肉眼判断，约 5-6 行空白以内）→ 未撑满则扩充摘要内容

### 一等奖与特等奖的关键区分
| 维度 | 一等奖 | 特等奖 |
|------|--------|--------|
| 理论深度 | 有推导、有引用 | 基于权威定理的非平凡应用或推广 |
| 模型创新 | 方法组合有新意 | 反套路设计 |
| 统一性 | 各问逻辑连贯 | 统一数学框架 |
| 验证 | 多方法交叉 + Sobol | 多维度交叉验证 |
| 写作 | 学术化 + 精确数值 | 数学美学 + 反直觉发现 |
| 学术规范 | 参考文献格式正确 | GB/T 7714 逐条格式验证 + 查重 ≤10% + AI 率 ≤15% |

### 数学美学（特等奖关键——结构层）

除格式规范外，特等奖论文的公式和结构需具备以下 7 项美学特质：
- **对称性利用**：利用问题内在对称性（置换/时间反演/空间对称）简化公式
- **对偶结构**：寻找原始-对偶关系（Lagrange/Fenchel/线性规划对偶），用对偶简化求解
- **变分原理**：微分方程模型重写为能量泛函极小化形式（更简洁优雅）
- **降维耦合**：多个结果用统一的生成函数/母函数/势函数表达
- **符号经济性**：总符号数 ≤30 个，消除冗余符号
- **公式结构对称**：相关公式间形成类比/递推/对偶的对称结构
- **渐近行为清晰**：关键公式的极限行为有直观解释

### GB/T 7714 参考文献格式验证（阶段 10 强制执行）

每篇参考文献必须通过 8 项检查：作者名格式（中英文分别规范）、文献类型标识 [J]/[M]/[C]/[D] 正确、期刊名全称/标准缩写、卷(期):页码格式、出版年 4 位数字、中英文混排标点、DOI 标注、无占位符。

### 英文论文（MCM/ICM）专属降 AI 策略

英文论文的 AI 检测特征与中文不同，需针对性处理：避免词汇重复（robust→resilient/stable）、打破句长规律（8-35 词交替）、减少 "Furthermore/Moreover" 模板连接、打破 4-5 句段落规律、使用具体数值替代笼统评价、适度使用主动语态。详见 `references/workspace.md` 三、英文论文降 AI 率专属策略。

**英文摘要写作**（MCM/ICM 40%+ 分数的决定因素）：按 6 要素标准结构（Problem Framing/Approach Overview/Per-Question Results/Cross-Validation/Innovation & Contribution/Conclusion）写作，每问精确数值+CI，≥1 问交叉验证，句长标准差 > 8 词。详见 `references/workspace.md` "MCM/ICM 英文摘要写作专项指南"。

### 特等奖强制门禁（10 项，全部通过才能提交）
1. 统一数学框架完整性（或不强求统一的合理退出）
2. 理论深度（至少 1 个基于权威定理的非平凡应用或推广）
3. 反直觉核心发现（至少 1 个）
4. 代码-论文一致性（5 类验证全部通过）
5. 幻觉零容忍（所有数值可溯源，所有文献可确认）
6. 格式合规（2026 官方规范）
7. 降重与降 AI 率（查重 ≤10%，AI 率 ≤15%）
8. 自评分数达标（≥95 分，各维度 ≥90%）
9. 技术路线图与问题分解图
10. 答辩准备（如适用）

---

## ⚠️ 八大失败模式

| # | 失败模式 | 表现 | 规避方法 |
|---|---------|------|---------|
| 1 | 创新点流于表面 | "改进了参数估计"、"组合了两种算法" | 必须有"为什么没人做"的解释 + 消融验证 |
| 2 | 模型与数据脱节 | 复杂模型但不比简单方法好 | 每个复杂模型必须与 baseline 对比 |
| 3 | 灵敏度走过场 | 仅单因素 ±10% | 必须 Sobol + 多因素交互 |
| 4 | 论文逻辑不连贯 | 各问像 4 篇小论文拼接 | 体现耦合关系和结果传递 |
| 5 | 结果"太好" | 所有指标远超 baseline | 如实报告局限性和失败尝试 |
| 6 | 摘要失败 | 以下任一情形导致摘要降档：①只写方法不写数值 ②数值无 CI/误差范围 ③方法名模糊（如"用机器学习方法"而非具体算法全称）④无交叉验证（每问仅单一方法）⑤关键词随意（数量不足/无核心方法词）⑥摘要超过一页 | 每问精确数值+CI+对比基准+具体方法全称+交叉验证+5个规范关键词+≤1页 |
| 7 | 理论分析缺失 | 只有代码和结果 | 每问至少一项理论分析 + 推导 |
| 8 | 假设无论证 | 假设列了一排但不解释合理性 | 每条假设有数据/文献支撑 |

---

## 📁 工作目录结构

```text
数学建模工作区/
├── 题目/                          ← 赛题原文 + 原始数据
├── 代码/                          ← 全部 Python 代码
│   ├── utils.py                   ← 公共工具（绘图/CI/路径）
│   ├── data_preprocessing.py      ← 数据预处理
│   ├── problem1.py ~ problem4.py  ← 各问求解
│   ├── sensitivity_analysis.py    ← 验证与灵敏度
│   ├── statistical_tests.py       ← 统计检验
│   ├── verify_consistency.py      ← 代码-论文一致性验证
│   └── requirements.txt           ← 依赖
├── 结果/figures/                  ← 图表（≥600dpi 或矢量图）
├── 论文/                          ← LaTeX 论文
├── 答辩/                          ← PPT（如需）
├── RESTORE.md                     ← 断点续接文件
├── 赛题分析.md
└── 建模计划.md
```

---

## 🔗 参考资源

- **完整操作手册**：`references/workspace.md` —— 包含每个阶段的详细操作指南、代码模板、质量评分表、竞赛适配器代码等
- **代码模板**：`templates/` 目录 —— 含 11 个完整可运行代码模板：
  - `utils.py` — 公共工具函数（绘图风格/置信区间/编码检测）
  - `problem_template.py` — 问题求解代码模板（7 个标准函数）
  - `data_preprocessing.py` — 数据预处理强制管线（编码检测→缺失值→异常值→质量报告）
  - `stage4_eda.py` — 深度 EDA 四层分析管线（单变量+双变量+多变量+网络）
  - `sensitivity_analysis.py` — 灵敏度分析（单因素Tornado+Sobol全局+MC稳健性+假设松弛退化曲线+Pareto前沿）
  - `statistical_tests.py` — 统计检验（t/Mann-Whitney/Friedman/Bonferroni/Power）
  - `extreme_test.py` — 极端情况测试（边界值+退化场景+噪声鲁棒性+备选方案对比）
  - `verify_consistency.py` — 代码-论文 5 类一致性验证
  - `create_ppt.py` — 答辩 PPT 自动生成（6-8 页标准结构+10 预答问题备忘）
  - `requirements.txt` — Python 依赖清单
- **LaTeX 模板**：`templates/main.tex` —— 符合 2026 年官方格式规范的完整论文模板

---

## 🛠️ Skills 调用指南

> 以下 Skills 可在特定阶段辅助执行。即使所有 Skill 都不工作，AI 必须自主完成全部流程。

| 阶段 | 推荐 Skill | Skill 不可用时的自主替代 |
|------|-----------|---------------|
| 项目规划 | `/writing-plans` | 自主编写任务分解+时间分配 |
| 赛题分析 | `/brainstorming` | 按阶段1思考框架自主执行 |
| 数据预处理 | `/data-analysis` | 自主编写 Python 代码 |
| 建模求解 | `/math-reasoning`, `/machine-learning`, `/scipy-best-practices` | 自主编写模型代码 |
| 代码审查 | `/requesting-code-review` | 按阶段7审查要点逐项自查 |
| 图表绘制 | `/matplotlib`, `/plotly` | 用 Python 代码生成图表 |
| 论文撰写 | `/paper-write-sci` | 按阶段8论文结构自主编写 LaTeX |
| 答辩 PPT | `/pptx` | 用 python-pptx 库生成 PPT |

---

## 📚 文献引用协议（强制执行，零幻觉保障）

AI 不能编造文献。全自主引用策略：

**AI 可自主引用的文献**
- 标准教科书（《数值分析》《统计学习方法》《最优化方法》）
- 著名算法原始论文（Louvain 2008, XGBoost 2016, SVM 1995, Sobol 2001）
- 数学定理/方法经典文献（Kuhn-Tucker 1951, Numerical Recipes, SciPy 2020）
- Python 科学计算生态文献（NumPy, SciPy, Matplotlib, Pandas 官方引用）

**文献量要求**：≥15 篇（含 ≥5 篇英文），全部从上述可自主引用类别中选取。无法自主确认的文献不引用，改用"论证替代引用"——通过独立推导说明观点，不依赖外部文献验证。

**⛔ 严格禁止**：编造作者/标题/期刊/DOI/页码、引用无法确认的文献。

---

## ⚡ 计算量动态调整（Sobol + 蒙特卡洛次数）

在阶段 5.0.2 先估算单次模型调用耗时，按此表动态调整：

| 单次模型耗时 | Sobol N | 蒙特卡洛 N | 说明 |
|------------|---------|-----------|------|
| < 0.01s | 2048 | 20000 | 全量执行 |
| 0.01-0.1s | 1024 | 10000 | 标准执行 |
| 0.1-1s | 512 | 5000 | 轻量执行 |
| 1-10s | 跳过(仅Tornado+双因素) | 1000 | 最小执行 |
| 10-60s | 跳过 | 500 | 仅 Tornado |
| > 60s | 跳过 | 跳过 | 局部灵敏度 ±10%，论文说明原因 |

**代理模型替代**（模型极慢时）：LHS 采样 100-500 参数组合 → 构建 GPR 代理模型 → 对代理模型执行 Sobol。

---

## 🔧 数据预处理强制管线

阶段 4 必须产出以下 4 个文件（不可跳过）：
1. **编码检测** → 多编码尝试 → 确认无乱码
2. **缺失值决策树**：<5%→KNN/多重插补；5-20%→多重插补+指示变量；20-50%→评估价值后决定；>50%→论文说明理由
3. **异常值**：IQR + Z-score + 孤立森林三方法交叉确认
4. **产出**：`data_manifest.json` + `preprocessed_data.csv` + `preprocessing_log.txt` + `data_quality_report.md`

---

## 🚫 工作规范（13 条强制执行纪律，全部有锁对应）

> 以下纪律来自原始工作区。违反任何一条的产出无效。

1. **不可跳步骤（锁2+锁13）**：13 个阶段按顺序执行，进入阶段 N 前 Grep 阶段进度追踪确认前置阶段 ✅完成。任一前置阶段 ⏳ → 禁止进入。
2. **逐问求解（锁3）**：每个问题独立一个 `.py` 文件，禁止合并。进入 problemN(N≥2) 前 Glob 前置问题产出 ≥2 图 + ≥1 CSV。
3. **保留中间结果（锁11）**：每步产出保存到文件，每子步骤有可验证产物（代码注释/日志/CSV）。
4. **代码-数据-论文一致（锁7）**：论文数据/图表/结论必须与代码运行结果一致。5 类一致性验证全通过才可提交。
5. **先验证后论文（锁4）**：阶段6和6.5全部通过后，才能进入阶段8。
6. **图表不可省（锁9）**：每问≥6张高质量图，全局≥12张。总量≥22张。≥600dpi，矢量图优先。3D图≥12张，强制双视角，文件名含 `3d` 标识。
7. **幻觉零容忍（锁7+原则9）**：论文中每个数值可溯源到代码输出。禁止"估算""大约"。
8. **文献真实性**：每篇必须来自可自主确认的来源。不确定的文献不引用，改用论证替代。
9. **不创建无关文件**。
10. **通读全文（锁10 CP6）**：论文完成后检查逻辑连贯性，CP6 回答核心贡献+创新点贡献排序。
11. **论文定稿前降重（锁15衔接）**：查重≤10%，AI率≤15%。
12. **提交包完整性（锁15）**：提交前逐项核对命名/格式/匿名/MD5，4/4项 ✅ 才可提交。
13. **附录可运行性（锁16）**：从 main.tex 提取代码 → 独立运行 → 逐值核对论文数值，全部一致才可提交。

---

## 🚨 紧急情况处置预案

> 同一方案连续尝试2次仍无进展→立即启用备选方案。

| 紧急情况 | 处置预案 |
|---------|---------|
| 模型结果与参考数据偏差大 | 最多校准2轮→接受当前精度→论文局限中诚实报告 |
| 算法不收敛 | 放宽收敛阈值→换鲁棒性更强的算法→数据标准化 |
| LaTeX编译失败≥3次 | pandoc生成Word→确保有docx提交 |
| 时间不足1小时 | 停止调参→确保docx+pdf双版本→直接提交 |
| Python 环境崩溃（pip install 失败/import 报错） | ① 检查完整路径 Python 可用性 ② 降级为纯 numpy/scipy 实现 ③ 跳过需要崩溃包的模型 → 论文 §7 诚实说明包不可用及替代方案 |
| 磁盘空间不足（无法保存图表/PDF） | ① 降低图表 dpi 至 150 应急 ② 删除中间缓存文件 ③ 压缩已有图表 → 优先保证 PDF 和 .zip 可生成 |
| 模板文件缺失（templates/ 不可用） | ① 按 SKILL.md 中描述的模板结构手动构建 ② 降级为最简 LaTeX（article + ctex + amsmath 三包即可） ③ 论文格式优先于美观 |
| workspace.md 不可读（文件损坏/路径错误） | ① 尝试 Grep 搜索关键词 ② 按 SKILL.md 内置的阶段执行指令直接执行（SKILL.md 已内嵌每个阶段的执行任务描述）③ 缺失的详细模板代码从 templates/ 目录 Read 获取 |

---

## 📊 图表规范速查

### 图表数量强制要求（冲刺档）

**全局图表（整体必需，不可省略）：**
- ≥1 张：**技术路线图**（含每个决策点的备选方案分支 + 阶段间数据流）—— §2
- ≥1 张：**方法谱系图**（Method Genealogy——文献分类定位，本文方法红色★标注）—— §2
- ≥1 张：**问题耦合关系有向图**（数据耦合/模型耦合/结果耦合/理论耦合）—— §2
- ≥3 张：**灵敏度分析图**（Tornado 单因素 + Sobol 全局指数 + MC 稳健性分布）—— §6
- ≥1 张：**假设松弛退化曲线**（2-3条核心假设，性能 vs 假设违反程度 0%→100%）—— §6
- ≥1 张：**Pareto 前沿图**（简单→中等→复杂→本文，X=复杂度 Y=性能）—— §6
- ≥1 张：**模型对比总表热力图**（本文 vs ≥3 篇文献 × ≥5 指标，三线表+热力色编码）—— §6
- ≥1 张：**文献定量对比图**（≥3 篇文献 × 多指标，3D 柱状或雷达图）—— §7
- ≥1 张：**What-if 情景对比图**（数据规模/约束/目标函数切换的影响）—— §7
- ≥1 张：**创新点贡献度瀑布图**（各创新点的增量贡献分解）—— §9

**每问图表（阶段 5 产出，≥6 张/问高质量图）：**
- ≥1 张：**模型原理/机理示意图**（模型结构框图、变量关系图、算法流程图——特等奖关键区分）
- ≥1 张：**消融实验对比图**（Full Model vs 去除创新点的量化对比，瀑布图或分组柱状图）
- ≥1 张：**核心结果可视化图**（含误差棒/CI + 显著性标注 *p<0.05 + 图中直接嵌入关键数值）
- ≥1 张：**不确定性/参数敏感性图**（参数扰动→结果波动范围）
- ≥1 张：**收敛曲线/残差诊断图**（迭代收敛历史 + 残差分布 + Q-Q 图）
- ≥1 张：**高级可视化图**（按问题类型选择：和弦图/桑基图/雷达图/小提琴图/脊线图/3D 图等）

**总量约束**：每问 ≥6 张 + 全局 ≥12 张（12类图表） → **论文图表总量 ≥ max(22, 12+6N) 张**（不含附录，N=问数）。图表密度 0.6-0.9 张/页，连续图表页 ≤2 页。**单题（N=1）最低要求：全局图表可增至14-16张以补足差额，确保总量 ≥22 张**。

### 插图/示意图高质量标准（冲刺档——评委 30 秒定档的关键）

示意图是评委形成第一印象的核心，必须达到以下标准：
- **格式**：矢量图绝对优先（PDF/SVG），位图 **≥600dpi**（PNG，关闭抗锯齿以保证线条锐利）
- **尺寸**：单张图 ≤3/4 页，半宽图 0.48-0.49\textwidth，3D 图 0.8-0.9\textwidth。两图并列优先，提高信息密度。**禁止用大图撑页数**——正文 30 页来自内容深度而非插图大小。详见 `references/workspace.md` "论文插图尺寸与紧凑性强制标准"。
- **配色**：**全局统一配色系统**——正文配色 = 图表配色，viridis 主色系贯穿全文。关键元素用高对比度突出，背景元素低饱和度退后。多系列图使用色盲友好调色板。
- **标注**：每个子图有独立标签(a)(b)(c)；图例完整且位置合理（不遮挡数据）；**图中直接嵌入关键数值**（用 `annotate`/`text` 将核心结果写在图内，让评委不看图注也能获取关键信息）
- **图注(caption)**：L4 分析式图注——三段式标准结构（标题15-25字→引导观察25-50字→归因与结论20-40字），5 要素齐全（编号+方法对应+核心数据发现+创新归因+不确定性说明），总计 60-120 字。禁止"如图所示""从上图可以看出"式空洞图注。
- **字体**：图中文字 **≥10pt**（坐标轴刻度）、标题 ≥12pt、图注 9pt（LaTeX caption 默认）
- **自检**（每张图出图后强制执行）：
  1. 缩放到论文实际尺寸后所有文字清晰可见
  2. 图表密度 0.6-0.9 张/页
  3. 连续图表页 ≤2 页（用文字段落穿插打破）
  4. 关闭图注后仅看图 → 能感知核心发现 = 通过；不能 = 重新设计
  5. 图中是否嵌入了关键数值？（至少 1 个具体数字）

### 🎨 3D 与高级插图规范（视觉冲击力——特等奖必备）

评委在 30 秒内形成第一印象，视觉冲击力决定论文的"档次感"。

**⛔ 3D 插图数量强制标准（锁 9 子约束，冲刺档）**：特等奖论文必须包含 **≥12 张高质量 3D 插图**，按以下位置分配：

| 论文位置 | 最低张数 | 推荐类型 | 核心目的 |
|---------|:--:|---------|---------|
| 问题分析/数据概览 | **≥2** | 3D 数据地形图 + 3D 散点矩阵分布图 | 展示数据多维结构，让评委立刻感知问题复杂性 |
| 每问建模求解 | **≥2/问** | 3D 曲面图（优化景观/参数空间）+ 3D 轨迹/流线/网络拓扑图 | 将数学模型可视化——评委看不懂公式但看得懂 3D 图 |
| 验证与灵敏度 | **≥3** | 双参数 Sobol 3D 交互曲面 + MC 稳健性 3D 分布图 + Pareto 前沿 3D 曲面 | 验证深度三维化——3D 灵敏度比二维 Tornado "高一个级别" |
| 讨论/方法对比 | **≥2** | 3D 柱状图（多方法×多指标×多场景）+ 文献定量对比 3D 柱状 | 多维视觉锚定：讨论中最重要的全局对比 |
| 结论与贡献 | **≥1** | 3D 瀑布图（创新点贡献度增量分解） | 收束全文的视觉高潮——直观展示每个创新的量化贡献 |

**信息增量验证（每张 3D 图必须通过）**：关闭图注后仅看图 → 能否感知到数据/模型的关键结构特征（峰值位置、曲面形状、聚类分布、拓扑关系）？能 = 有效 3D 图 → 保留；不能 = 纯装饰 → 不合格 → 重新设计。

**3D 文件命名规范**：所有 3D 图文件名必须含 `3d` 标识——`figN_3d_<类型>.png`（如 `fig2_3d_surface.png`、`figS2_3d_sobol.png`），便于 Glob 统计和门禁验证。

以下类型应在合适场景下使用：

**3D 可视化类型（按推荐优先级）**：

| 类型 | 适用场景 | 推荐库 | 效果要求 |
|------|---------|--------|---------|
| **3D 柱状图** | 多方案/多方法对比、多维度效果展示 | `matplotlib.mplot3d` / `plotly` | 柱体半透明(alpha=0.7)，带深度阴影 |
| **3D 曲面图** | 双参数灵敏度分析、优化景观可视化 | `plotly.graph_objects` / `mayavi` | 渐变色映射(viridis/plasma)，等高线投影 |
| **3D 散点图** | 高维数据 PCA/t-SNE 前三主成分、聚类结果 | `plotly.express.scatter_3d` | 颜色编码分类，点大小反映权重 |
| **3D 网络/拓扑图** | 复杂网络结构、耦合关系、传播路径 | `plotly` / `pyvis` | 节点颜色+大小编码属性，边粗细编码强度 |
| **3D 流线/轨迹图** | 动力系统相轨迹、优化收敛路径、ODE 解曲线 | `matplotlib.quiver` / `plotly` | 渐变色调编码时间/速度 |
| **3D 密度/体积图** | 概率密度分布、热力场、浓度扩散 | `plotly.isosurface` / `mayavi` | 半透明等值面，多层级叠加 |
| **3D 地形/景观图** | 目标函数景观、解空间可视化 | `plotly.surface` + 光照 | 山影效果(shading)，俯视+侧视双视角 |
| **3D 瀑布图** | 创新点贡献增量分解、误差溯源 | `plotly.waterfall` / `matplotlib` 自定义 | 逐项累加，起始值→中间增量→最终值，正负贡献分色 |

**3D 配色高级方案**：

```python
# 推荐渐变色映射（科学+美观）
'viridis'      # 蓝→绿→黄，色盲友好，通用首选
'plasma'       # 深紫→红→黄，热烈醒目
'inferno'      # 黑→红→黄，适合强调极值区域
'magma'        # 黑→紫→橙，高贵冷色调
'turbo'        # 彩虹全色谱，鲜艳但需谨慎（色盲不友好）
'cividis'      # 蓝→黄，全色盲友好

# 3D 场景光照设置
dict(lightposition=(0, 0, 1),    # 顶光源，突出峰值
     lightcolor='white',
     lighting=dict(ambient=0.3,   # 环境光 30%
                   diffuse=0.7,   # 漫反射 70%
                   specular=0.5,  # 镜面高光
                   roughness=0.2))
```

**3D 绘图强制规范（冲刺档——每张 3D 图必须满足）**：

- **视角**：**强制双视角**——每张 3D 图至少提供 45° 主视图 + 90° 俯视图两视角，防止信息因视角遮挡丢失。两个视角可并排放在同一张图的两个子图中
- **透明度**：遮挡严重时使用 alpha=0.6~0.8 半透明，确保后方结构可见
- **投影**：3D 曲面必须带 XY/XZ/YZ 平面投影（等高线），辅助精确读数
- **色条(colorbar)**：必须标注，含物理量名称+单位
- **网格线**：淡灰色(gridcolor='lightgray', alpha=0.3)，不抢主视觉
- **光照**：使用顶光源 `lightposition=(0, 0, 1)`，环境光 30% + 漫反射 70% + 镜面高光，提升立体感和专业度
- **交互导出**：优先生成 HTML 交互版（plotly），同时导出 PDF 矢量版 → 论文使用 PDF，答辩使用 HTML
- **图中嵌入数值**：在 3D 曲面上标注峰值、谷值、拐点等关键位置的坐标数值
- **⛔ 禁忌**：禁止纯装饰性 3D（无信息增量的旋转立方体/球体）；禁止 3D 饼图（信息失真）；禁止单视角 3D（信息可能因遮挡而丢失）

**高级插图类型（冲刺档强制——每问至少选 1 种作为第 6 张图）**：

| 类型 | 适用场景 | 工具 |
|------|---------|------|
| **桑基图(Sankey)** | 能量/物质/信息流向 | `plotly.graph_objects.Sankey` |
| **和弦图(Chord)** | 节点间双向交互关系 | `holoviews` / `plotly` |
| **雷达图(Radar)** | 多指标综合评分对比 | `plotly.line_polar` |
| **热力图(Heatmap)** | 矩阵数据、相关性、时间×空间 | `seaborn.heatmap` / `plotly.imshow` |
| **瀑布图(Waterfall)** | 增量分解、误差溯源 | `plotly.waterfall` |
| **小提琴图(Violin)** | 分布对比（优于箱线图） | `seaborn.violinplot` |
| **脊线图(Ridgeline)** | 多组分布随时间/条件变化 | `joypy` |

### 数据图表质量标准（冲刺档）

- **配色系统**：全局统一配色——正文强调色 = 图表主色调，viridis 系贯穿全文。色盲友好调色板 `['#0072B2','#E69F00','#009E73','#CC79A7','#D55E00']`（Wong 2011）。3D 图使用 viridis/plasma/inferno 渐变色映射
- **分辨率**：位图 **≥600dpi**（`plt.savefig(dpi=600)`），矢量图优先（PDF/SVG）
- **字体**：坐标轴刻度字号 **≥10pt**，标题字号 **≥12pt**，图中标注文字 ≥9pt，确保 A4 印刷后清晰可读
- **子图编号**：(a)(b)(c)(d) 格式，使用 `subcaption` 宏包，子图注 ≥20 字
- **误差棒**：所有数值结果图标注误差棒或 95% CI
- **显著性**：用 * 标注（*p<0.05, **p<0.01, ***p<0.001），图注中说明检验方法
- **占版率**：每张图占版面 **≥1/6 页**（约 5cm × 7cm），避免过小看不清。子图组合不超过 4 行 × 3 列
- **图中嵌入数值**：关键图中直接标注核心数值（`plt.annotate` / `plt.text`），让评委不看图注也能获取关键信息
- **双视角 3D**：每张 3D 图至少提供 45° 主视图 + 90° 俯视图，防止信息遮挡
- **白边优化**：所有图导出时使用 `bbox_inches='tight', pad_inches=0.1`，去除无效白边，最大化有效绘图面积
- **坐标轴完整性**：所有坐标轴含物理量名称 + 单位（如"时间 t (s)"），对数轴标注底数
- **图例位置**：图例不遮挡数据区域，优先放在图内空白处或图外右侧

---

## 📋 执行检查清单（AI 自检用）

**每阶段结束后**：
- [ ] 本阶段产出是否包含模板中没有的实质性内容？
- [ ] 本阶段核心结论是否有代码/数据/推导支撑？
- [ ] 如果评委只看本阶段产出，能否理解核心贡献？
- [ ] 所有数值是否有代码输出佐证？（幻觉自检）
- [ ] 是否有无法溯源到代码输出的数值或无法确认的文献？

**最终提交前**：
- [ ] 特等奖强制门禁 10 项全部通过
- [ ] 代码-论文一致性 5 类验证全部通过
- [ ] 幻觉风险评分 HR = 0
- [ ] 论文质量自评 ≥ 95 分，各维度 ≥ 90%
